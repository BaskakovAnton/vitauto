Витавто — пакет для отправки (2026-09-17)

Открывать в браузере (двойной клик или локальный сервер):

1. process.html     — интерактивный процесс B5 + оценка ТЗ (?view=estimate)
2. index.html       — матрица прав
3. approve.html     — протокол апрувов (встреча)
4. protocol-map.html — карта протокола + смета пакетами
5. bt.html          — бизнес-требования (проект)
6. ft.html          — функциональные требования (проект)
7. br-fr-analysis.html — разбор источников

Данные:
- protocol-lock.json — зафиксированный протокол (снимок 45)
- protocol-lock-slim.json — компактная копия для карты

Печать БТ/ФТ в PDF: в bt.html / ft.html кнопка «Печать / PDF».

Все ссылки между файлами относительные — держите файлы в одной папке.
