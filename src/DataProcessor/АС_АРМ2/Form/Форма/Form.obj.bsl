#Область ОбработчикиФормы

&НаКлиенте
Процедура ДобавитьСтрокиПодборНоменклатуры(Команда)
	ТекущиеДанныеСтрока = Элементы.ДанныеАРМОтображение.ТекущиеДанные;
	Если ТекущиеДанныеСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Если ТекущиеДанныеСтрока.Свойство("ГруппировкаСтроки") Тогда
		ТекстСообщения = "Для копирования строки встаньте на строку, а не на группировку";
		ПоказатьОповещениеПользователя("Внимание!", , ТекстСообщения);
		Возврат;
	КонецЕсли;
	ДобавитьСтрокиПодборНоменклатурыНаСервере(ТекущиеДанныеСтрока);
	ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.Арм_ДанныеЗакупокИПродаж"));
	Элементы.ДанныеАРМОтображение.Обновить();
	Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьСтрокиПодборНоменклатурыЗакупка(Команда)
	ТекущиеДанныеСтрока = Элементы.ДанныеАРМОтображениеЗакупки.ТекущиеДанные;
	Если ТекущиеДанныеСтрока = Неопределено Тогда
		Возврат;
	КонецЕсли;
	Если ТекущиеДанныеСтрока.Свойство("ГруппировкаСтроки") Тогда
		ТекстСообщения = "Для копирования строки встаньте на строку, а не на группировку";
		ПоказатьОповещениеПользователя("Внимание!", , ТекстСообщения);
		Возврат;
	КонецЕсли;
	ДобавитьСтрокиПодборНоменклатурыНаСервере(ТекущиеДанныеСтрока);
	ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.Арм_ДанныеЗакупокИПродаж"));
	Элементы.ДанныеАРМОтображение.Обновить();
	Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
КонецПроцедуры

&НаСервере
Процедура ДобавитьСтрокиПодборНоменклатурыНаСервере(ТекущиеДанныеСтрока) 
	
	Если ТекущиеДанныеСтрока <> Неопределено Тогда
		
		НоваяЗапись = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьМенеджерЗаписи();
		НоваяЗапись.ДокументЗаказПокупателя = ТекущиеДанныеСтрока.ДокументЗаказПокупателя;
		НоваяЗапись.ДокументЗаказПоставщика = ТекущиеДанныеСтрока.ДокументЗаказПоставщика;
		НоваяЗапись.ДокументПриходнаяНакладная = ТекущиеДанныеСтрока.ДокументПриходнаяНакладная;
		НоваяЗапись.НоменклатураКлиента = ТекущиеДанныеСтрока.НоменклатураКлиента;
		НоваяЗапись.КоличествоКлиента = ТекущиеДанныеСтрока.КоличествоКлиента;
		НоваяЗапись.КлючСвязиСтрок = ТекущиеДанныеСтрока.КлючСвязиСтрок;
		НоваяЗапись.ИдентификаторЗаписи = Строка(Новый УникальныйИдентификатор);
		НоваяЗапись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Черновик;
		НоваяЗапись.ВидОперации = ТекущиеДанныеСтрока.ВидОперации;
		
		Попытка
			НоваяЗапись.Записать();
		Исключение
			ТекстОшибки = СтрШаблон(
				"Ошибка записи в АРМ-регист. Копирование строки: %1", 
				КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
			);
			ЗаписьЖурналаРегистрации(
				"АРМ.ВыборВидОперации", 
				УровеньЖурналаРегистрации.Ошибка, 
				, 
				, 
				ТекстОшибки
			);	
		КонецПопытки;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИнвертироватьФлагНаСервере(Знач ИдентификаторЗаписи, Знач ЗначениеФлага)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		
		Запись = Набор[0]; 
		
		Если ЗначениеЗаполнено(Запись.АвторСтроки) И Запись.АвторСтроки <> ПараметрыСеанса.ТекущийПользователь Тогда
			Возврат; 
		КонецЕсли;
		
		Запись.ОтметкаСтроки = Не ЗначениеФлага;
		
		Если (Не ЗначениеФлага) = Ложь Тогда
			Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка();
		Иначе
			Запись.АвторСтроки = ПараметрыСеанса.ТекущийПользователь;
		КонецЕсли;
		
		Попытка
			Набор.Записать();
		Исключение
			ТекстОшибки = СтрШаблон(
				"Ошибка записи флажка для ID %1: %2", 
				ИдентификаторЗаписи, 
				КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
			);
			ЗаписьЖурналаРегистрации("АРМ.ОшибкаГалки", УровеньЖурналаРегистрации.Ошибка, , , ТекстОшибки);
		КонецПопытки;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)

	Элементы.ДанныеАРМОтображение.ТекущийЭлемент = Элементы.ДанныеАРМОтображениеНоменклатураКлиента;
	ПодключитьОбработчикОжидания("ОбновитьСписокПоТаймеру", 30);
	ОтображениеКнопокДокументов(Элементы.ДанныеАРМОтображениеЗакупки);
	
КонецПроцедуры

&НаКлиенте
Процедура ВРаботе(Команда)
	
	ВыполнитьОсновнуюОбработку(Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ВРаботеЗавершение(Результат, ДополнительныеПараметры) Экспорт
	Если Результат = КодВозвратаДиалога.Да Тогда
		Элементы.Заказать.Доступность = Ложь;
		Элементы.ПровестиЗаказПокупателя.Доступность = Ложь;
		ВыполнитьОсновнуюОбработку(Истина);
	Иначе
		СброситьБлокировкуИнтерфейса();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьОсновнуюОбработку(Знач ФлагРазрешитьСплит)

	СтатусыДляМенеджера = Новый Массив;
	СтатусыДляМенеджера.Добавить(ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.ВРаботе"));
	СтатусыДляМенеджера.Добавить(ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.Черновик"));
	СтатусыДляМенеджера.Добавить(ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.Собран"));
	МассивКлючейДляФона = Арм_ОбщегоНазначенияАРМ.ПолучитьМассивОтмеченныхКлючейСвязи(СтатусыДляМенеджера);
	Если МассивКлючейДляФона.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;

	Элементы.Заказать.Доступность = Ложь; 
	Элементы.ПровестиЗаказПокупателя.Доступность = Ложь;
	ВременноЗаблокированныеКлючи.Очистить();
	ВременноЗаблокированныеКлючи.ЗагрузитьЗначения(МассивКлючейДляФона);
	УправлятьИнтерфейснымЛокомНаСервере(МассивКлючейДляФона, Истина);
	ОбновитьСписокПоТаймеру();
	
	Попытка
		ЗапуститьФоновоеЗаданиеНаСервере(МассивКлючейДляФона, ФлагРазрешитьСплит); 
		ПодключитьОбработчикОжидания("ПроверитьСтатусФоновогоЗадания", 2);
	Исключение
		СброситьБлокировкуИнтерфейса();
		ОбщегоНазначенияКлиент.СообщитьПользователю("Критическая ошибка при попытке запустить фоновый поток.");
	КонецПопытки;
	
КонецПроцедуры

&НаСервере
Процедура УправлятьИнтерфейснымЛокомНаСервере(МассивКлючей, Знач ВключитьЛок)
	
	КоллекцияДляСКД = Новый СписокЗначений;
	Если ВключитьЛок Тогда
		КоллекцияДляСКД.ЗагрузитьЗначения(МассивКлючей);
	КонецЕсли;
	
	ДанныеАРМОтображение.Параметры.УстановитьЗначениеПараметра("ВременноЗаблокированныеКлючи", КоллекцияДляСКД);
	ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ВременноЗаблокированныеКлючи", КоллекцияДляСКД);
	
КонецПроцедуры

&НаСервере
Процедура ЗапуститьФоновоеЗаданиеНаСервере(Знач МассивКлючей, Знач ФлагРазрешитьСплит)
	
	ЗапросЗаказы = Новый Запрос;
	ЗапросЗаказы.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Рег.ДокументЗаказПокупателя КАК Заказ
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Рег
		|ГДЕ
		|	Рег.КлючСвязиСтрок В(&МассивКлючей)
		|	И НЕ Рег.Удаление";
	ЗапросЗаказы.УстановитьПараметр("МассивКлючей", МассивКлючей);
	МассивЗаказов = ЗапросЗаказы.Выполнить().Выгрузить().ВыгрузитьКолонку("Заказ");
	
	АдресРезультата = ПоместитьВоВременноеХранилище(Неопределено, ЭтотОбъект.УникальныйИдентификатор);
	
	ПараметрыПроцедуры = Новый Структура;
	ПараметрыПроцедуры.Вставить("ТекущийПользователь", ПараметрыСеанса.ТекущийПользователь);
	ПараметрыПроцедуры.Вставить("МассивКлючей",        МассивКлючей);
	ПараметрыПроцедуры.Вставить("МассивЗаказов",       МассивЗаказов); 
	ПараметрыПроцедуры.Вставить("АдресРезультата",     АдресРезультата);
	ПараметрыПроцедуры.Вставить("РазрешитьСплит",      ФлагРазрешитьСплит);
	
	МассивПараметров = Новый Массив;
	МассивПараметров.Добавить(ПараметрыПроцедуры);
	
	Задание = ФоновыеЗадания.Выполнить(
		"Арм_ДанныеЗакупокИПродажФон.СформироватьЗаказыФоновоеЗадание", 
		МассивПараметров, 
		"АРМ_Заказы_" + Строка(ПараметрыСеанса.ТекущийПользователь.УникальныйИдентификатор())
	);
	
	ЭтотОбъект.ИдентификаторФоновогоЗадания   = Задание.УникальныйИдентификатор;
	ЭтотОбъект.АдресРезультатаФоновогоЗадания = АдресРезультата;
	
КонецПроцедуры

&НаКлиенте
Процедура ПроверитьСтатусФоновогоЗадания()
	
	СостояниеЗадания = ПроверитьСтатусФоновогоНаСервере(ИдентификаторФоновогоЗадания);
	
	Если СостояниеЗадания = "Active" Или СостояниеЗадания = "Активно" Или СостояниеЗадания = "Задание выполняется" Тогда
		Возврат; 
	КонецЕсли;
	
	Если СостояниеЗадания = "Completed" Или СостояниеЗадания = "Завершено" Или СостояниеЗадания = "Задание выполнено" Тогда
		
		РезультатОбработки = ПолучитьИзВременногоХранилищаНаСервере(АдресРезультатаФоновогоЗадания);
		
		Если РезультатОбработки <> Неопределено 
			И РезультатОбработки.СтатусВалидации = "ТребуетсяПодтверждениеМенеджера" Тогда
			
			ОтключитьОбработчикОжидания("ПроверитьСтатусФоновогоЗадания");
			УдалитьИзВременногоХранилища(АдресРезультатаФоновогоЗадания);			
			ТекстВопроса = СтрШаблон(
				"Выбраны не все позиции по заказам (отмечено %1 шт. из %2 шт.). 
				|Оставшиеся строки будут перенесены в новые заказы. 
				|Продолжить?", 
				РезультатОбработки.ВсегоВыбраноШтук, 
				РезультатОбработки.ВсегоВБазеШтук
			);
			
			ПараметрыОповещения = Новый Структура("РазрешитьСплит", Истина);
			ОписаниеОповещения  = Новый ОписаниеОповещения("ВРаботеЗавершение", ЭтотОбъект, ПараметрыОповещения);
			
			ПоказатьВопрос(ОписаниеОповещения, ТекстВопроса, РежимДиалогаВопрос.ДаНет, , КодВозвратаДиалога.Да, "Сплит заказа");
			Возврат;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ОтключитьОбработчикОжидания("ПроверитьСтатусФоновогоЗадания");
	СброситьБлокировкуИнтерфейса();
	
	Если СостояниеЗадания = "Completed" Или СостояниеЗадания = "Завершено" Или СостояниеЗадания = "Задание выполнено" Тогда
		
		Если РезультатОбработки <> Неопределено Тогда
			
			Если РезультатОбработки.Успешно Тогда
				ПоказатьОповещениеПользователя("Успешно", , "Все документы оформлены!");
				ОбновитьСписокПоТаймеру();
			Иначе
				ТекстОшибкиВалидации = РезультатОбработки.ТекстОшибки;
				ПоказатьПредупреждение(, ТекстОшибкиВалидации);
				ОбновитьСписокПоТаймеру();
			КонецЕсли;
			
		Иначе
			ПоказатьПредупреждение(, "Документы обработаны, но ответ от сервера не получен. Обновите список вручную.");
			ОбновитьСписокПоТаймеру();
		КонецЕсли;
	Иначе
		ПоказатьПредупреждение(, "Критический сбой потока на сервере 1С. Изменения отменены. Проверьте Журнал Регистрации.");
		ОбновитьСписокПоТаймеру();
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПроверитьСтатусФоновогоНаСервере(Знач ИДЗадания)
	
	Задание = ФоновыеЗадания.НайтиПоУникальномуИдентификатору(ИДЗадания);
	 Если Задание = Неопределено Тогда
		Возврат "НеНайдено";
	КонецЕсли;
	
	Возврат Строка(Задание.Состояние); 
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИзВременногоХранилищаНаСервере(Знач Адрес)
	
	Возврат ПолучитьИзВременногоХранилища(Адрес);
	
КонецФункции

&НаКлиенте
Процедура СброситьБлокировкуИнтерфейса()
	Элементы.Заказать.Доступность = Истина;
	Элементы.ПровестиЗаказПокупателя.Доступность = Истина;
	ВременноЗаблокированныеКлючи.Очистить();
	УправлятьИнтерфейснымЛокомНаСервере(Новый Массив, Ложь);
КонецПроцедуры

&НаСервере
Функция ПодготовитьИПровестиЗаказыНаСервере(МассивКлючейСвязи) /// удалить
	
	Пользователь = ПараметрыСеанса.ТекущийПользователь;
	
	ЗапросВалидация = Новый Запрос;
	ЗапросВалидация.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Т.Номенклатура.Наименование КАК Номенклатура
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.КлючСвязиСтрок В(&МассивКлючей)
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Т.Номенклатура = ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	Т.Номенклатура.Наименование
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.КлючСвязиСтрок В(&МассивКлючей)
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Т.Количество <= 0
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	Т.Номенклатура.Наименование
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.КлючСвязиСтрок В(&МассивКлючей)
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Т.Цена <= 0";

	ЗапросВалидация.УстановитьПараметр("МассивКлючей", МассивКлючейСвязи);
	Результат = ЗапросВалидация.Выполнить();
	
	Если Не Результат.Пустой() Тогда
		Выборка = Результат.Выбрать();
		ТекстОшибки = СтрШаблон("Заказ не создан! В связанных продажах не заполнена цена или товар: %1", Символы.ПС);
		Пока Выборка.Следующий() Цикл
			ТекстОшибки = СтрШаблон("%1 - %2 %3", ТекстОшибки, Выборка.Номенклатура, Символы.ПС);
		КонецЦикла;
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		Возврат Ложь; 
	КонецЕсли;
	
	ПровестиЗаказПокупателяНаСервере();
	
	ЗапросЗаказы = Новый Запрос;
	ЗапросЗаказы.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Т.ДокументЗаказПокупателя КАК Заказ
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.КлючСвязиСтрок В(&МассивКлючей)";
	ЗапросЗаказы.УстановитьПараметр("МассивКлючей", МассивКлючейСвязи);
	ВыборкаЗаказов = ЗапросЗаказы.Выполнить().Выбрать();
	
	Пока ВыборкаЗаказов.Следующий() Цикл
		Если Не ЗначениеЗаполнено(ВыборкаЗаказов.Заказ) Тогда 
			Продолжить; 
		КонецЕсли;
		
		ДокОбъект = ВыборкаЗаказов.Заказ.ПолучитьОбъект();
		Если ДокОбъект <> Неопределено Тогда
			Если НЕ ДокОбъект.Записать(РежимЗаписиДокумента.Проведение) Тогда
				ПолныйТекстОшибки = СтрШаблон("Заказ %1 не провелся! Список ошибки ниже ", Строка(ВыборкаЗаказов.Заказ));
				ОбщегоНазначения.СообщитьПользователю(ПолныйТекстОшибки);
				Возврат Ложь;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Возврат Истина;
	
КонецФункции

&НаКлиенте
Процедура ДанныеАРМОтображениеПриАктивизацииЯчейки(Элемент)
	
	//Если Элемент.ТекущийЭлемент = Неопределено Тогда
	//	Возврат;
	//КонецЕсли;

	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда 
		Возврат; 
	КонецЕсли;
	
	Если ТекущиеДанные.Свойство("СтрокаЗаблокирована") И ТекущиеДанные.СтрокаЗаблокирована Тогда 
		Возврат; 
	КонецЕсли;	
	
	Если Не ТекущиеДанные.Свойство("ИдентификаторЗаписи") Или Не ТекущиеДанные.Свойство("ОтметкаСтроки") Тогда
		Возврат;
	КонецЕсли;
	Если Элемент.ТекущийЭлемент.Имя = "ДанныеАРМОтображениеОтметкаСтроки" 
		Или Элемент.ТекущийЭлемент.Имя = "ДанныеАРМОтображениеЗакупкиОтметкаСтроки" Тогда
		ИнвертироватьФлагНаСервере(ТекущиеДанные.ИдентификаторЗаписи, ТекущиеДанные.ОтметкаСтроки);
		ОбновитьСписокПоТаймеру();
		Если Элемент.ТекущийЭлемент.Имя = "ДанныеАРМОтображениеОтметкаСтроки" Тогда
			Элементы.ДанныеАРМОтображение.ТекущийЭлемент = Элементы.ДанныеАРМОтображениеНоменклатураКлиента;
		ИначеЕсли Элемент.ТекущийЭлемент.Имя = "ДанныеАРМОтображениеЗакупкиОтметкаСтроки" Тогда
			Элементы.ДанныеАРМОтображениеЗакупки.ТекущийЭлемент = Элементы.ДанныеАРМОтображениеЗакупкиНоменклатураКлиента;	
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура НастроитьОформлениеБлокировкиФоном()
	
	МассивСписков = Новый Массив;
	МассивСписков.Добавить(ДанныеАРМОтображение);
	МассивСписков.Добавить(ДанныеАРМОтображениеЗакупки);
	
	Для Каждого ТекущийСписок Из МассивСписков Цикл
		
		УсловноеОформлениеСписок = ТекущийСписок.УсловноеОформление;
		
		ЭлементОформления = УсловноеОформлениеСписок.Элементы.Добавить();
		
		ЭлементОформления.Оформление.УстановитьЗначениеПараметра("ЦветФона",    WebЦвета.Серый);
		ЭлементОформления.Оформление.УстановитьЗначениеПараметра("Доступность", Ложь);
		ЭлементУсловия = ЭлементОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ЭлементУсловия.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("СтрокаЗаблокирована");
		ЭлементУсловия.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ЭлементУсловия.ПравоеЗначение = Истина; 
		ЭлементУсловия.Использование  = Истина;
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Попытка
		НастроитьУсловноеОформление();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		МассивВидовОперацияЗакупка = Новый Массив;
		МассивВидовОперацияЗакупка.Добавить(Перечисления.Арм_ВидыОпераций.Закупка);
		МассивВидовОперацияЗакупка.Добавить(Перечисления.Арм_ВидыОпераций.ПустаяСсылка());
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("МассивВидОперации", МассивВидовОперацияЗакупка);

		МассивВидовОперацияПродажа = Новый Массив;
		МассивВидовОперацияПродажа.Добавить(Перечисления.Арм_ВидыОпераций.Продажа);
		МассивВидовОперацияПродажа.Добавить(Перечисления.Арм_ВидыОпераций.ПустаяСсылка());
		ДанныеАРМОтображение.Параметры.УстановитьЗначениеПараметра("МассивВидОперации", МассивВидовОперацияПродажа);

		Период.Вариант = ВариантСтандартногоПериода.ЭтотМесяц;
		ДанныеАРМОтображение.Параметры.УстановитьЗначениеПараметра("ВременноЗаблокированныеКлючи", Новый Массив);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ВременноЗаблокированныеКлючи", Новый Массив);

		// Параметры отбора списка Закупки — обязательны при открытии (иначе «Не задано значение параметра»).
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказу1С", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ДокументЗаказПокупателя", Документы.ЗаказПокупателя.ПустаяСсылка());
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказуКлиента", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("НомерЗаказаКлиента", "");
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоКлючуСвязи", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("КлючСвязиСтрок", "");
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		НастроитьОформлениеБлокировкиФоном();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		УстановитьПериодНаСервере();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		ОбновитьФлагГруппировки();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		ОбновитьФлагТолькоСЗакупками();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		ОбновитьФлагСкрытьОтгруженные();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		ОбновитьФлагСкрытьВычеркнутыеЗаписи();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	Конецпопытки;
	Попытка
		УстановитьОформлениеПриВводеЦеныКоэффициента();
	Исключение
		ЗаписатьОшибкуУсловногоОформления();
	КонецПопытки;
	Попытка
		УстановитьЗначокМашинки();
	Исключение
		
	КонецПопытки;
	
КонецПроцедуры

Процедура УстановитьЗначокМашинки()
	
	ЭлементМашинка = УсловноеОформление.Элементы.Добавить();
	
	ЭлементМашинка.Оформление.УстановитьЗначениеПараметра("Картинка", БиблиотекаКартинок.Арм_Грузовичок);
	
	УсловиеСтатуса = ЭлементМашинка.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	УсловиеСтатуса.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображение.СтатусСтроки");
	УсловиеСтатуса.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	УсловиеСтатуса.ПравоеЗначение = Перечисления.Арм_СтатусыАРМ.Заказано;
	
	ПолеСтатуса = ЭлементМашинка.Поля.Элементы.Добавить();
	ПолеСтатуса.Поле = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображениеСтатусСтроки");
	
КонецПроцедуры

&НаСервере
Процедура УстановитьОформлениеПриВводеЦеныКоэффициента()
	
	ОформлениеЦена = УсловноеОформление.Элементы.Добавить();
	ОформлениеЦена.Оформление.УстановитьЗначениеПараметра("Шрифт", Новый Шрифт(,, Истина));
	
	УсловиеЦена = ОформлениеЦена.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	УсловиеЦена.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображение.ТипИзмененияЦены");
	УсловиеЦена.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	УсловиеЦена.ПравоеЗначение = 1;
	
	ПолеЦена = ОформлениеЦена.Поля.Элементы.Добавить();
	ПолеЦена.Поле = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображениеЦена");
	
	ОформлениеКоэф = УсловноеОформление.Элементы.Добавить();
	ОформлениеКоэф.Оформление.УстановитьЗначениеПараметра("Шрифт", Новый Шрифт(,, Истина)); 
	
	УсловиеКоэф = ОформлениеКоэф.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	УсловиеКоэф.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображение.ТипИзмененияЦены");
	УсловиеКоэф.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	УсловиеКоэф.ПравоеЗначение = 2;
	
	ПолеКоэф = ОформлениеКоэф.Поля.Элементы.Добавить();
	ПолеКоэф.Поле = Новый ПолеКомпоновкиДанных("ДанныеАРМОтображениеКоэффициент");
	
КонецПроцедуры

&НаСервере
Процедура ЗаписатьОшибкуУсловногоОформления()
	
	ТекстОшибкиИнтерфейса = СтрШаблон(
		"Сбой при сборке условного оформления АРМа: %1", 
		КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
	);
	
	ЗаписьЖурналаРегистрации(
		"АРМ.ОшибкаИнтерфейса", 
		УровеньЖурналаРегистрации.Предупреждение, 
		, 
		, 
		ТекстОшибкиИнтерфейса
	);
		
КонецПроцедуры

&НаСервере
Процедура ОбновитьФлагСкрытьОтгруженные()
	
	СохраненноеЗначение = ХранилищеОбщихНастроек.Загрузить("АРМ_Автосервис_Настройки", "СкрытьОтгруженные");
	СкрытьОтгруженные = ?(СохраненноеЗначение = Неопределено, Истина, СохраненноеЗначение);
	СкрытьОтгруженныеНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьФлагГруппировки()
	
	ФлагЗаказ1С      = ХранилищеОбщихНастроек.Загрузить("АРМ_НастройкаГруппировки", "ФлагГруппировкиЗаказ1С");
	ФлагЗаказКлиента = ХранилищеОбщихНастроек.Загрузить("АРМ_НастройкаГруппировки", "ФлагГруппировкиЗаказКлиента");
	
	СгруппироватьПоЗаказуПокупателя   = ?(ФлагЗаказ1С = Неопределено,      Ложь, ФлагЗаказ1С);
	ГруппироватьПоНомеруЗаказаКлиента = ?(ФлагЗаказКлиента = Неопределено, Ложь, ФлагЗаказКлиента);
	
	Если СгруппироватьПоЗаказуПокупателя И ГруппироватьПоНомеруЗаказаКлиента Тогда
		ГруппироватьПоНомеруЗаказаКлиента = Ложь;
	КонецЕсли;
	
	УстановитьГруппировкуАРМНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьФлагТолькоСЗакупками()
	СохраненноеЗначение = ХранилищеОбщихНастроек.Загрузить("АС_АРМ_Снабжение", "ТолькоСЗакупками");
    Если СохраненноеЗначение <> Неопределено Тогда
        ТолькоСЗакупками = СохраненноеЗначение;
        УстановитьОтборПоЗакупкам(); 
    КонецЕсли;	
КонецПроцедуры

&НаСервере
Процедура ОбновитьФлагСкрытьВычеркнутыеЗаписи()
	
	ЗначениеИзХранилища = ХранилищеОбщихНастроек.Загрузить("АРМ_НастройкиДанных", "НеПоказыватьВычеркнутые");
	НеПоказыватьВычеркнутыеПозиции = ?(ЗначениеИзХранилища = Неопределено, Ложь, ЗначениеИзХранилища);
	СкрытьПоказатьВычеркнутыеПозицииНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура НастроитьУсловноеОформление()
	
	Автор = ПараметрыСеанса.ТекущийПользователь;
	ДанныеАРМОтображение.УсловноеОформление.Элементы.Очистить();
	
	// общее оформление для всего списка (текст мельче)
	ЭлементОбщий = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	// шрифт 
	ЭлементОбщий.Оформление.УстановитьЗначениеПараметра("Шрифт", Новый Шрифт("MS Shell Dlg", 8, Ложь, Ложь, Ложь));
	ЭлементОбщий.Оформление.УстановитьЗначениеПараметра("ЦветТекста", Новый Цвет(60, 60, 60));
	
	// вычеркнутые
	ЭлементВычеркнутые = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ШрифтЗачеркнутый = Новый Шрифт(,,,,, Истина); // Последний параметр - Зачеркивание
	ЭлементВычеркнутые.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтЗачеркнутый);
	ЭлементВычеркнутые.Оформление.УстановитьЗначениеПараметра("ЦветТекста", Новый Цвет(160, 160, 160)); //ЦветаСтиля.ТекстВторостепеннойНадписи);
	
	// условие по удаленным
	ВычеркнутыеСтроки = ЭлементВычеркнутые.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ВычеркнутыеСтроки.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Удаление");
	ВычеркнутыеСтроки.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ВычеркнутыеСтроки.ПравоеЗначение = Истина;
	
	// вычеркнутые закупки
	ЭлементВычеркнутыеЗакупки = ДанныеАРМОтображениеЗакупки.УсловноеОформление.Элементы.Добавить();
	ШрифтЗачеркнутый = Новый Шрифт(,,,,, Истина); // Последний параметр - Зачеркивание
	ЭлементВычеркнутыеЗакупки.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтЗачеркнутый);
	ЭлементВычеркнутыеЗакупки.Оформление.УстановитьЗначениеПараметра("ЦветТекста", Новый Цвет(160, 160, 160)); //ЦветаСтиля.ТекстВторостепеннойНадписи);
	
	// условие по удаленным
	ВычеркнутыеСтрокиЗакупки = ЭлементВычеркнутыеЗакупки.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ВычеркнутыеСтрокиЗакупки.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Удаление");
	ВычеркнутыеСтрокиЗакупки.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ВычеркнутыеСтрокиЗакупки.ПравоеЗначение = Истина;
	
	//
	ЭлементПродажи = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементПродажи.Оформление.УстановитьЗначениеПараметра("ЦветФона", Новый Цвет(255, 224, 178)); 
	
	//
	ОтборПродажи = ЭлементПродажи.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборПродажи.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ВидОперации");
	ОтборПродажи.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборПродажи.ПравоеЗначение = Перечисления.Арм_ВидыОпераций.Закупка;
	//
	
	// настройка для своих галочек
	ЭлементСвои = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементСвои.Оформление.УстановитьЗначениеПараметра("ЦветФона", Новый Цвет(220, 255, 220)); 
	
	// условие 1: ОтметкаСтроки = Истина
	ОтборСвои = ЭлементСвои.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборСвои.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтметкаСтроки");
	ОтборСвои.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборСвои.ПравоеЗначение = Истина;
	
	// условие 2: Автор = ТекущийПользователь
	ОтборСвоиАвтор = ЭлементСвои.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборСвоиАвтор.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("АвторСтроки");
	ОтборСвоиАвтор.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборСвоиАвтор.ПравоеЗначение = Автор;
	
	// настройках для чужих галочек
	ЭлементЧужие = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементЧужие.Оформление.УстановитьЗначениеПараметра("ЦветТекста", Новый Цвет(160, 160, 160));	
	// условие 1: ОтметкаСтроки = Истина
	ОтборЧужие = ЭлементЧужие.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЧужие.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтметкаСтроки");
	ОтборЧужие.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЧужие.ПравоеЗначение = Истина;
	
	// условие 2: Автор НЕ РАВНО ТекущийПользователь
	ОтборЧужиеАвтор = ЭлементЧужие.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЧужиеАвтор.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("АвторСтроки");
	ОтборЧужиеАвтор.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЧужиеАвтор.ПравоеЗначение = Автор;
	
	// оформление закупок
	ЭлементРучныеЗакупки = ДанныеАРМОтображениеЗакупки.УсловноеОформление.Элементы.Добавить();
	ЭлементРучныеЗакупки.Оформление.УстановитьЗначениеПараметра("ЦветФона", Новый Цвет(255, 224, 178));
	ОтборРучныеЗакупки = ЭлементРучныеЗакупки.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборРучныеЗакупки.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("РучнаяПравка");
	ОтборРучныеЗакупки.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборРучныеЗакупки.ПравоеЗначение = Истина;
	
	ЭлементОформления = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементОформления.Оформление.УстановитьЗначениеПараметра("Текст", "0");

	// условие:
	КомпоновкаДанных = ЭлементОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	КомпоновкаДанных.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОстатокДляСтроки");
	КомпоновкаДанных.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ПолеОформления = ЭлементОформления.Поля.Элементы.Добавить();
	ПолеОформления.Поле = Новый ПолеКомпоновкиДанных("ОстатокДляСтроки");
	
	// дефицит
	ЭлементДефицит = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементДефицит.Оформление.УстановитьЗначениеПараметра("ЦветТекста", Новый Цвет(0, 0, 255));

	ОтборЭлементДефицит = ЭлементДефицит.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлементДефицит.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ЕстьДефицит");
	ОтборЭлементДефицит.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлементДефицит.ПравоеЗначение = Истина;  
	
	// добавляем второе условие: Партия не заполнена
	ОтборПартияПустая = ЭлементДефицит.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборПартияПустая.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Партия");
	ОтборПартияПустая.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;
	
	// незаполненное +
	ЦветОшибки = Новый Цвет(255, 235, 235);

	// 1. Поле "Количество" в продажах
	ЭлементКол = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементКол.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	ОтборКол = ЭлементКол.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборКол.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Количество");
	ОтборКол.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборКол.ПравоеЗначение = 0;

	ПолеОфКол = ЭлементКол.Поля.Элементы.Добавить();
	ПолеОфКол.Поле = Новый ПолеКомпоновкиДанных("Количество"); 

	// 2. Поле "Цена" в продажах (ОТДЕЛЬНЫМ элементом)
	ЭлементЦена = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементЦена.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	ОтборЦена = ЭлементЦена.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЦена.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Цена");
	ОтборЦена.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЦена.ПравоеЗначение = 0;

	ПолеОфЦена = ЭлементЦена.Поля.Элементы.Добавить();
	ПолеОфЦена.Поле = Новый ПолеКомпоновкиДанных("Цена");
	
	// Подсветка пустой Номенклатуры в продажах
	ЭлементНом = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементНом.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	// Условие: Номенклатура не заполнена
	ОтборНом = ЭлементНом.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборНом.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Номенклатура");
	ОтборНом.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	// Применяем именно к полю Номенклатура
	ПолеНом = ЭлементНом.Поля.Элементы.Добавить();
	ПолеНом.Поле = Новый ПолеКомпоновкиДанных("Номенклатура");
	
	// дата отгрузки
	ЭлементДатаОтгрузки = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементДатаОтгрузки.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	ОтборДатаОтгрузки = ЭлементДатаОтгрузки.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборДатаОтгрузки.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ДатаОтгрузки");
	ОтборДатаОтгрузки.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ПолеОфДатаОтгрузки = ЭлементДатаОтгрузки.Поля.Элементы.Добавить();
	ПолеОфДатаОтгрузки.Поле = Новый ПолеКомпоновкиДанных("ДатаОтгрузки"); 
	
	// Номер Отгрузки
	ЭлементНомерОтгрузки = ДанныеАРМОтображение.УсловноеОформление.Элементы.Добавить();
	ЭлементНомерОтгрузки.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	ОтборНомерОтгрузки = ЭлементНомерОтгрузки.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборНомерОтгрузки.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("НомерОтгрузки");
	ОтборНомерОтгрузки.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ПолеОфНомерОтгрузки = ЭлементНомерОтгрузки.Поля.Элементы.Добавить();
	ПолеОфНомерОтгрузки.Поле = Новый ПолеКомпоновкиДанных("НомерОтгрузки");

	// 2. Поля "ДатаПоступления", "Цена" и "Поставщик" в закупках
	ЭлементЗакупки = ДанныеАРМОтображениеЗакупки.УсловноеОформление.Элементы.Добавить();
	ЭлементЗакупки.Оформление.УстановитьЗначениеПараметра("ЦветФона", ЦветОшибки);

	// Условие: Только для тех строк, где стоит галка "Заказать"
	ОтборГалка = ЭлементЗакупки.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборГалка.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтметкаСтроки");
	ОтборГалка.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборГалка.ПравоеЗначение = Истина;

	// Группа условий "ИЛИ" для пустых полей
	ГруппаИЛИ = ЭлементЗакупки.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаИЛИ.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли;

	// Цена не заполнена
	ОтборЦена = ГруппаИЛИ.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЦена.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Цена");
	ОтборЦена.ВидСравнения  = ВидСравненияКомпоновкиДанных.МеньшеИлиРавно;
	ОтборЦена.ПравоеЗначение = 0; 
	
	// Поставщик не заполнен
	ОтборПоставщик = ГруппаИЛИ.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборПоставщик.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Поставщик");
	ОтборПоставщик.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено; 
	
	// Договор не заполнен
	ОтборДоговр = ГруппаИЛИ.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборДоговр.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Договор");
	ОтборДоговр.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	// Применяем подсветку конкретно к этим колонкам
	ПолеЦена = ЭлементЗакупки.Поля.Элементы.Добавить();
	ПолеЦена.Поле = Новый ПолеКомпоновкиДанных("Цена");
	
	ПолеПоставщик = ЭлементЗакупки.Поля.Элементы.Добавить();
	ПолеПоставщик.Поле = Новый ПолеКомпоновкиДанных("Поставщик");
	
	ПолеДоговор = ЭлементЗакупки.Поля.Элементы.Добавить();
	ПолеДоговор.Поле = Новый ПолеКомпоновкиДанных("Договор");
	// незаполненное -
	
КонецПроцедуры 

&НаКлиенте
Процедура ОбновитьСписокПоТаймеру()
	Элементы.ДанныеАРМОтображение.Обновить();
	Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
КонецПроцедуры

&НаКлиенте
Процедура ДанныеАРМОтображениеВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
    СтандартнаяОбработка = Ложь;
	
    ДанныеСтроки = Элемент.ТекущиеДанные;
	Если ДанныеСтроки = Неопределено Тогда 
		Возврат; 
	КонецЕсли;
	
	Если Не ДанныеСтроки.Свойство("СтатусСтроки") Тогда
		Возврат;
	КонецЕсли;
	
	Если ДанныеСтроки.СтатусСтроки <> ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.ВРаботе") 
		И ДанныеСтроки.СтатусСтроки <> ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.Собран")
		И ДанныеСтроки.СтатусСтроки <> ПредопределенноеЗначение("Перечисление.Арм_СтатусыАРМ.Черновик") Тогда
		ТекстСообщения = СтрШаблон(
			"Невозможно редактировать строку на статусе %1",
			ДанныеСтроки.СтатусСтроки
		);
		ПоказатьОповещениеПользователя("Внимание!", , ТекстСообщения);
		Возврат;
	КонецЕсли;
	
	Если ДанныеСтроки.Свойство("СтрокаЗаблокирована") И ДанныеСтроки.СтрокаЗаблокирована Тогда 
		Возврат; 
	КонецЕсли;
	
	Если ТипЗнч(ВыбраннаяСтрока) = Тип("СтрокаГруппировкиДинамическогоСписка") Тогда
		Если ВыбраннаяСтрока.ИмяГруппировки = "ДокументЗаказПокупателя" Тогда
			ПоказатьЗначение(, ВыбраннаяСтрока.Ключ);	
		КонецЕсли;
		Возврат;
	КонецЕсли;
	
	Если ДанныеСтроки.Свойство("ГруппировкаСтроки") Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ДанныеСтроки.Свойство("ИдентификаторЗаписи") Тогда
		Возврат;
	КонецЕсли;
    
	Если Поле.Имя = "ДанныеАРМОтображениеНоменклатура" //Или Поле.Имя = "ДанныеАРМОтображениеЗакупкиНоменклатура" 
		Или Поле.Имя = "ДанныеАРМОтображениеНоменклатураАртикул" Тогда
		
		ДопПараметры = Новый Структура;
		ДопПараметры.Вставить("ИдентификаторЗаписи", ДанныеСтроки.ИдентификаторЗаписи);
		ДопПараметры.Вставить("ИмяПоля", "Номенклатура");
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораЗначенияНоменклатура", ЭтотОбъект, ДопПараметры);
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора", Истина); 
		ПараметрыФормы.Вставить("ЗакрыватьПриВыборе", Истина);
		
		ОткрытьФорму("Справочник.Номенклатура.ФормаВыбора", ПараметрыФормы, Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеЗакупкиНоменклатура" Тогда
		
		ТекстСообщения = "Если Вы хотите закупить товар-аналог, в Закупках " 
			+ "нажмите ""Скопировать продажу"" и выберите нужный товар";
		ПоказатьОповещениеПользователя("Внимание!", , ТекстСообщения);
		Возврат;
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеКомментарийКСтроке" Тогда
		
		Если Не ДанныеСтроки.Свойство("КомментарийКСтроке") Тогда
			Возврат;
		КонецЕсли;
		СтрокаДляВвода = ДанныеСтроки.КомментарийКСтроке;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаКомментария", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводСтроки(ОписаниеОповещения, СтрокаДляВвода, "Введите комментарий к строке:", 150, Ложь);
		
	//ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеГРЗКлиента" Тогда
	//	
	//	Если Не ДанныеСтроки.Свойство("ГРЗКлиента") Тогда
	//		Возврат;
	//	КонецЕсли;
	//	СтрокаДляВвода = ДанныеСтроки.ГРЗКлиента;
	//	ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаГРЗКлиента", ЭтотОбъект, ДанныеСтроки);
	//	ПоказатьВводСтроки(ОписаниеОповещения, СтрокаДляВвода, "Введите ГРЗ:", 10, Ложь);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеКоличество" Или Поле.Имя = "ДанныеАРМОтображениеЗакупкиКоличество" Тогда
		
		Если Не ДанныеСтроки.Свойство("Количество") Тогда
			Возврат;
		КонецЕсли;
		ЧислоДляВвода = ДанныеСтроки.Количество;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаКоличества", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводЧисла(ОписаниеОповещения, ЧислоДляВвода, "Введите количество:", 15, 3);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеЦена" Или Поле.Имя = "ДанныеАРМОтображениеЗакупкиЦена" Тогда
		
		Если Не ДанныеСтроки.Свойство("Цена") Тогда
			Возврат;
		КонецЕсли;
		ЧислоДляВвода = ДанныеСтроки.Цена;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаЦены", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводЧисла(ОписаниеОповещения, ЧислоДляВвода, "Введите цену:", 15, 3);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеКоэффициент" Тогда
		
		Если Не ДанныеСтроки.Свойство("Коэффициент") Тогда
			Возврат;
		КонецЕсли;
		ЧислоДляВвода = ДанныеСтроки.Коэффициент;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаКоэффициента", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводЧисла(ОписаниеОповещения, ЧислоДляВвода, "Введите коэффициент:", 15, 3);
	
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеВидОперации" Или Поле.Имя = "ДанныеАРМОтображениеЗакупкиВидОперации" Тогда
		
		Возврат;
		
		Если ЗначениеЗаполнено(ДанныеСтроки.Номенклатура) 
			И ДанныеСтроки.ВидОперации = ПредопределенноеЗначение("Перечисление.Арм_ВидыОпераций.Закупка") Тогда
			Возврат;
		КонецЕсли;
		
		Если ДанныеСтроки.ИдентификаторЗаписи <> ДанныеСтроки.КлючСвязиСтрок Тогда
			ВыбранноеЗначение = Неопределено;
			ТипВыбора = Новый ОписаниеТипов("ПеречислениеСсылка.Арм_ВидыОпераций");
			Оповещение = Новый ОписаниеОповещения("ЗавершениеВыбораВидаОперации", ЭтотОбъект, ДанныеСтроки);
			ПоказатьВводЗначения(Оповещение, Неопределено, "Выберите вид операции", ТипВыбора);
		КонецЕсли;
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеПартия" Тогда
		
		СтандартнаяОбработка = Ложь;
		Если ДанныеСтроки = Неопределено Тогда
			Возврат;
		КонецЕсли;
		
		Если Не ДанныеСтроки.Свойство("Номенклатура") Тогда
			Возврат;
		КонецЕсли;
		
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораПартии", ЭтотОбъект, ДанныеСтроки);
		
		ПараметрыОткрытия = Новый Структура;
		ПараметрыОткрытия.Вставить("Номенклатура", ДанныеСтроки.Номенклатура);
		ПараметрыОткрытия.Вставить("РежимВыбора", Истина);
		ОткрытьФорму("Справочник.ПартииНоменклатуры.Форма.Арм_ФормаВыбора", ПараметрыОткрытия, Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеЗакупкиПоставщик" Тогда
		    
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораПоставщика", ЭтотОбъект, ДанныеСтроки); 
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора", Истина); 
		ПараметрыФормы.Вставить("ЗакрыватьПриВыборе", Истина);
		ОткрытьФорму("Справочник.Контрагенты.ФормаСписка", ПараметрыФормы , Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеВидУслуги" Тогда
		    
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораУслуги", ЭтотОбъект, ДанныеСтроки); 
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора", Истина); 
		ПараметрыФормы.Вставить("ЗакрыватьПриВыборе", Истина);
		ОткрытьФорму("Справочник.Арм_ВидыУслуги.ФормаВыбора", ПараметрыФормы , Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеФормаОплаты" Тогда
		    
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораФормыОплаты", ЭтотОбъект, ДанныеСтроки); 
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("РежимВыбора", Истина); 
		ПараметрыФормы.Вставить("ЗакрыватьПриВыборе", Истина);
		ОткрытьФорму("Справочник.Арм_ФормыОплаты.ФормаВыбора", ПараметрыФормы , Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеЗакупкиДоговор" Тогда
		
		Если Не ДанныеСтроки.Свойство("Поставщик") Тогда
			Возврат;
		КонецЕсли;
		
		Оповещение = Новый ОписаниеОповещения("ПослеВыбораДоговора", ЭтотОбъект, ДанныеСтроки);
		
		ПараметрыОткрытия = Новый Структура;
		ПараметрыОткрытия.Вставить("Владелец", ДанныеСтроки.Поставщик);
		ПараметрыОткрытия.Вставить("РежимВыбора", Истина);
		ОткрытьФорму("Справочник.ДоговорыКонтрагентов.Форма.ФормаВыбора", ПараметрыОткрытия, Элемент, , , , Оповещение);
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеДатаОтгрузки" Тогда
		
		Если Не ДанныеСтроки.Свойство("ДатаОтгрузки") Тогда
			Возврат;
		КонецЕсли;
		
		Если ДанныеСтроки.Свойство("Партия") И Не ЗначениеЗаполнено(ДанныеСтроки.Партия) Тогда
			Возврат;
		КонецЕсли;
		
		ДатаДляВвода = ?(ЗначениеЗаполнено(ДанныеСтроки.ДатаОтгрузки), ДанныеСтроки.ДатаОтгрузки, ТекущаяДата());
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаДатыОтгрузки", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводДаты(ОписаниеОповещения, ДатаДляВвода, "Укажите дату отгрузки:", ЧастиДаты.Дата);	
		
	ИначеЕсли Поле.Имя = "ДанныеАРМОтображениеНомерОтгрузки" Тогда
		
		Если Не ДанныеСтроки.Свойство("НомерОтгрузки") Тогда
			Возврат;
		КонецЕсли;
		
		Если ДанныеСтроки.Свойство("Партия") И Не ЗначениеЗаполнено(ДанныеСтроки.Партия) Тогда
			Возврат;
		КонецЕсли;
		
		СтрокаДляВвода = ДанныеСтроки.НомерОтгрузки;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаНомераОтгрузки", ЭтотОбъект, ДанныеСтроки);
		ПоказатьВводСтроки(ОписаниеОповещения, СтрокаДляВвода, "Введите номер отгрузки:", 10, Ложь); 
		
	Иначе
		
		Возврат;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЗаказПоставщику(Команда)
	СоздатьЗаказПоставщикуНаСервере();
КонецПроцедуры

&НаСервере
Процедура СоздатьЗаказПоставщикуНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Т.ИдентификаторЗаписи КАК ИдентификаторЗаписи,
		|	Т.ДокументЗаказПокупателя КАК ДокументЗаказПокупателя,
		|	Т.КлючСвязиСтрок КАК КлючСвязиСтрок,
		|	Т.Номенклатура КАК Номенклатура,
		|	Т.Количество КАК Количество,
		|	Т.Поставщик КАК Поставщик,
		|	Т.Договор КАК Договор,
		|	Т.Цена КАК Цена
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.ОтметкаСтроки
		|	И Т.АвторСтроки = &АвторСтроки
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Закупка)
		|	И Т.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.Черновик)
		|	И НЕ Т.Удаление";
	
	Запрос.УстановитьПараметр("АвторСтроки", ПараметрыСеанса.ТекущийПользователь);
	РезультатЗапроса = Запрос.Выполнить();
	ТЗ_Выделенные = РезультатЗапроса.Выгрузить();
	
	Если ТЗ_Выделенные.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	///==/////СоздатьЗаказыПоставщикамНаСервере(ТЗ_Выделенные);
		
КонецПроцедуры

&НаКлиенте
Процедура ЗавершениеВыбораВидаОперации(Результат, ДанныеСтроки) Экспорт
    
    Если Результат <> Неопределено Тогда
        
        УстановитьВидОперацииНаСервере(ДанныеСтроки, Результат);
        Элементы.ДанныеАРМОтображение.Обновить(); 
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
        
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ПериодПриИзменении(Элемент)
	
	УстановитьПериодНаСервере();
	
КонецПроцедуры 

&НаСервере
Процедура УстановитьВидОперацииНаСервере(ДанныеСтроки, НовыйВидОперации)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Запись.ВидОперации = НовыйВидОперации;
		Попытка
			Набор.Записать();
			
			// пересчет остатков для строки
			Если ЗначениеЗаполнено(ДанныеСтроки.Номенклатура) Тогда
				Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(ДанныеСтроки.Номенклатура, ДанныеСтроки.ДокументЗаказПокупателя);
			КонецЕсли;
			
		Исключение
			ТекстОшибки = СтрШаблон(
				"Ошибка записи в АРМ-регистр. ВидОперации: %1, Описание: %2", 
				Строка(ДанныеСтроки.ИдентификаторЗаписи), 
				КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
			);
			ЗаписьЖурналаРегистрации(
				"АРМ.ВыборВидОперации", 
				УровеньЖурналаРегистрации.Ошибка, 
				, 
				, 
				ТекстОшибки
			);
		КонецПопытки;
	КонецЕсли;
        
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораПартии(ВыбранноеЗначение, ДанныеСтроки) Экспорт
    
    Если ВыбранноеЗначение <> Неопределено Тогда
        ЗафиксироватьПартиюНаСервере(ДанныеСтроки, ВыбранноеЗначение);
        Элементы.ДанныеАРМОтображение.Обновить();
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораДоговора (ВыбранноеЗначение, ДанныеСтроки) Экспорт
	
	Если ВыбранноеЗначение <> Неопределено Тогда
        ЗафиксироватьДоговорНаСервере(ДанныеСтроки, ВыбранноеЗначение);
        Элементы.ДанныеАРМОтображение.Обновить();
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораПоставщика(ВыбранноеЗначение, ДанныеСтроки) Экспорт
    
    Если ВыбранноеЗначение <> Неопределено Тогда
        ЗафиксироватьПоставщикаНаСервере(ДанныеСтроки, ВыбранноеЗначение);
		Элементы.ДанныеАРМОтображение.Обновить();
        Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораУслуги(ВыбранноеЗначение, ДанныеСтроки) Экспорт
    
    Если ВыбранноеЗначение <> Неопределено Тогда
        ЗафиксироватьВидУслугиНаСервере(ДанныеСтроки, ВыбранноеЗначение);
		Элементы.ДанныеАРМОтображение.Обновить();
        Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораФормыОплаты(ВыбранноеЗначение, ДанныеСтроки) Экспорт
    
    Если ВыбранноеЗначение <> Неопределено Тогда
        ЗафиксироватьФормуОплатыНаСервере(ДанныеСтроки, ВыбранноеЗначение);
		Элементы.ДанныеАРМОтображение.Обновить();
        Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ЗафиксироватьПоставщикаНаСервере(ДанныеСтроки, ВыбранноеЗначение)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Запись.Поставщик = ВыбранноеЗначение;
		Набор.Записать();
	Иначе
		ЗаписьЖурналаРегистрации("АРМ.ВыборПоставщика",,,, "Не найден ID: " + ДанныеСтроки.ИдентификаторЗаписи);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗафиксироватьВидУслугиНаСервере(ДанныеСтроки, ВыбранноеЗначение)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Запись.ВидУслуги = ВыбранноеЗначение;
		Набор.Записать();
	Иначе
		ЗаписьЖурналаРегистрации("АРМ.ВыборВидаУслуги",,,, "Не найден ID: " + ДанныеСтроки.ИдентификаторЗаписи);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗафиксироватьФормуОплатыНаСервере(ДанныеСтроки, ВыбранноеЗначение)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Запись.ФормаОплаты = ВыбранноеЗначение;
		Набор.Записать();
	Иначе
		ЗаписьЖурналаРегистрации("АРМ.ВыборФормыОплаты",,,, "Не найден ID: " + ДанныеСтроки.ИдентификаторЗаписи);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ЗафиксироватьПартиюНаСервере(ДанныеСтроки, ВыбраннаяПартия)
	
    Менеджер = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьМенеджерЗаписи();
    ЗаполнитьЗначенияСвойств(Менеджер, ДанныеСтроки);
    Менеджер.Прочитать();
    
    Если Менеджер.Выбран() Тогда
        
        Менеджер.Партия = ВыбраннаяПартия;
        Менеджер.РучнаяПравкаПартии = Истина;
        
        ЗапросЦены = Новый Запрос;
        ЗапросЦены.Текст = 
            "ВЫБРАТЬ
            |	ВЫБОР
            |		КОГДА ЗапасыОстатки.КоличествоОстаток <> 0
            |			ТОГДА ЗапасыОстатки.СуммаОстаток / ЗапасыОстатки.КоличествоОстаток
            |		ИНАЧЕ 0
            |	КОНЕЦ КАК Цена,
            |	ПартииНоменклатуры.ВладелецПартии КАК Поставщик
            |ИЗ
            |	РегистрНакопления.Запасы.Остатки(, Партия = &Партия) КАК ЗапасыОстатки
            |		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ПартииНоменклатуры КАК ПартииНоменклатуры
            |		ПО ЗапасыОстатки.Партия = ПартииНоменклатуры.Ссылка";
        
        ЗапросЦены.УстановитьПараметр("Партия", ВыбраннаяПартия);
        ВыборкаЦены = ЗапросЦены.Выполнить().Выбрать();
        
        Если ВыборкаЦены.Следующий() Тогда
            Менеджер.Себестоимость = ВыборкаЦены.Цена;
			Менеджер.Поставщик = ВыборкаЦены.Поставщик;
        КонецЕсли;
		
		Попытка
        	Менеджер.Записать();
		Исключение
			ЗаписьЖурналаРегистрации("АРМ.ВыборПартии", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки());
		КонецПопытки;
        
        Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(Менеджер.Номенклатура, Менеджер.ДокументЗаказПокупателя); 
        
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ЗафиксироватьДоговорНаСервере(ДанныеСтроки, ВыбранныйДоговор)
	
    Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Запись.Договор = ВыбранныйДоговор;
		Набор.Записать();
	Иначе
		ЗаписьЖурналаРегистрации("АРМ. Выбор договора", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки());
	КонецЕсли; 
        
КонецПроцедуры

&НаКлиенте
Процедура ДанныеАРМОтображениеПриАктивизацииСтроки(Элемент)
	
	//ТекущиеДанные = Элемент.ТекущиеДанные;
	//Если ТекущиеДанные <> Неопределено Тогда
	//	Если ТекущиеДанные.Свойство("ДокументЗаказПокупателя") Тогда
	//    	ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра(
	//			"ДокументЗаказПокупателя", 
	//			ТекущиеДанные.ДокументЗаказПокупателя
	//		);
	//	КонецЕсли;
	//	
	//	Если ТекущиеДанные.Свойство("Удаление") И ТекущиеДанные.Удаление Тогда
	//		Элементы.ВычеркнутьТовар.Заголовок = "Восстановить товар";
	//		Элементы.ВычеркнутьТовар.Картинка = БиблиотекаКартинок.ВосстановитьЗначения; 
	//	КонецЕсли;
	//	Если ТекущиеДанные.Свойство("Удаление") И Не ТекущиеДанные.Удаление Тогда 
	//		Элементы.ВычеркнутьТовар.Заголовок = "Вычеркнуть товар";
	//		Элементы.ВычеркнутьТовар.Картинка = БиблиотекаКартинок.Удалить;
	//	КонецЕсли;
	//	
	//	Если ТекущиеДанные.Свойство("ОтметкаСтроки") И ТекущиеДанные.ОтметкаСтроки Тогда
	//		Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Отменить выделение всех";
	//		Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.СнятьФлажки; 
	//	КонецЕсли;
	//	Если ТекущиеДанные.Свойство("ОтметкаСтроки") И Не ТекущиеДанные.ОтметкаСтроки Тогда 
	//		Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Выделить все";
	//		Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.УстановитьФлажки;
	//	КонецЕсли;
	//	
	//	ОтображениеКнопокДокументов(Элементы.ДанныеАРМОтображениеЗакупки);
	//Иначе
	//    ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ДокументЗаказПокупателя", "");
	//КонецЕсли;   
	//
	//ОбновитьИтогиВыделения(); 
	
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные <> Неопределено Тогда
		
		Флаг1С      = Ложь;
		ФлагКлиента = Ложь;
		ФлагКлюча   = Ложь;
		
		ЗначениеЗаказа1С      = ПредопределенноеЗначение("Документ.ЗаказПокупателя.ПустаяСсылка");
		ЗначениеЗаказаКлиента = "";
		ЗначениеКлючаСвязи    = "";
		
		Если СгруппироватьПоЗаказуПокупателя Тогда
			Если ТекущиеДанные.Свойство("ДокументЗаказПокупателя") Тогда
				Флаг1С           = Истина;
				ЗначениеЗаказа1С = ТекущиеДанные.ДокументЗаказПокупателя;
			КонецЕсли;
		ИначеЕсли ГруппироватьПоНомеруЗаказаКлиента Тогда
			Если ТекущиеДанные.Свойство("НомерЗаказаКлиента") Тогда
				ФлагКлиента           = Истина;
				ЗначениеЗаказаКлиента = ТекущиеДанные.НомерЗаказаКлиента;
			КонецЕсли;
		Иначе
			Если ТекущиеДанные.Свойство("КлючСвязиСтрок") Тогда
				ФлагКлюча          = Истина;
				ЗначениеКлючаСвязи = ТекущиеДанные.КлючСвязиСтрок;
			КонецЕсли;
		КонецЕсли;
		
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказу1С",         Флаг1С);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ДокументЗаказПокупателя", ЗначениеЗаказа1С);
		
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказуКлиента", 	 ФлагКлиента);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("НомерЗаказаКлиента",   	 ЗначениеЗаказаКлиента);
		
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоКлючуСвязи",     	 ФлагКлюча);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("КлючСвязиСтрок",          ЗначениеКлючаСвязи);
		
		Если ТекущиеДанные.Свойство("Удаление") И ТекущиеДанные.Удаление Тогда
			Элементы.ВычеркнутьТовар.Заголовок = "Восстановить товар";
			Элементы.ВычеркнутьТовар.Картинка = БиблиотекаКартинок.ВосстановитьЗначения; 
		КонецЕсли;
		Если ТекущиеДанные.Свойство("Удаление") И Не ТекущиеДанные.Удаление Тогда 
			Элементы.ВычеркнутьТовар.Заголовок = "Вычеркнуть товар";
			Элементы.ВычеркнутьТовар.Картинка = БиблиотекаКартинок.Удалить;
		КонецЕсли;
		
		Если ТекущиеДанные.Свойство("ОтметкаСтроки") И ТекущиеДанные.ОтметкаСтроки Тогда
			Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Отменить выделение всех";
			Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.СнятьФлажки; 
		КонецЕсли;
		Если ТекущиеДанные.Свойство("ОтметкаСтроки") И Не ТекущиеДанные.ОтметкаСтроки Тогда 
			Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Выделить все";
			Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.УстановитьФлажки;
		КонецЕсли;
		
		ОтображениеКнопокДокументов(Элементы.ДанныеАРМОтображениеЗакупки);
	Иначе
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказу1С", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ДокументЗаказПокупателя", ПредопределенноеЗначение("Документ.ЗаказПокупателя.ПустаяСсылка"));
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоЗаказуКлиента", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("НомерЗаказаКлиента", "");
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("ОтборПоКлючуСвязи", Ложь);
		ДанныеАРМОтображениеЗакупки.Параметры.УстановитьЗначениеПараметра("КлючСвязиСтрок", "");
	КонецЕсли;   
	
	ОбновитьИтогиВыделения();
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьИтогиВыделения();
	ВыделенныеСтроки = Элементы.ДанныеАРМОтображение.ВыделенныеСтроки;
	ПоказатьСуммуКоличество(ВыделенныеСтроки);	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьСуммуКоличество(ВыделенныеСтроки)
	
	ЭтотОбъект.Количество = 0;
    ЭтотОбъект.Сумма = 0;
    
    Если ВыделенныеСтроки.Количество() > 0 Тогда
        
		Для Каждого Идентификатор Из ВыделенныеСтроки Цикл 
			
			СтрокаДанных = Элементы.ДанныеАРМОтображение.ДанныеСтроки(Идентификатор);
			
			Если СтрокаДанных <> Неопределено Тогда
				Если СтрокаДанных.Свойство("Количество") И СтрокаДанных.Свойство("Цена") Тогда
					ЭтотОбъект.Количество = ЭтотОбъект.Количество + СтрокаДанных.Количество;
					ЭтотОбъект.Сумма = ЭтотОбъект.Сумма + СтрокаДанных.Цена;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
        
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаКомментария(Результат, ДанныеСтроки) Экспорт
    
    Если Результат = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    ДанныеСтроки.КомментарийКСтроке = Результат;
    СохранитьКомментарийВРегистр(ДанныеСтроки.ИдентификаторЗаписи, Результат);
	ОбновитьСписокПоТаймеру();
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаГРЗКлиента(Результат, ДанныеСтроки) Экспорт
    
    Если Результат = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    ДанныеСтроки.ГРЗКлиента = Результат;
    СохранитьГРЗВРегистр(ДанныеСтроки.ИдентификаторЗаписи, Результат);
	ОбновитьСписокПоТаймеру();
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаКоличества(Результат, ДанныеСтроки) Экспорт
	Если Результат <> Неопределено Тогда
        ДанныеСтроки.Количество = Результат; 
        ИзменитьКоличествоНаСервере(ДанныеСтроки); 
        ОбновитьСписокПоТаймеру();
    КонецЕсли;
КонецПроцедуры 

&НаКлиенте
Процедура ПослеВводаНомераОтгрузки(Результат, ДанныеСтроки) Экспорт
	Если Результат <> Неопределено Тогда
        ДанныеСтроки.НомерОтгрузки = Результат; 
        ИзменитьНомерОтгрузкиНаСервере(ДанныеСтроки); 
        ОбновитьСписокПоТаймеру();
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаДатыОтгрузки(Результат, ДанныеСтроки) Экспорт
	Если Результат <> Неопределено Тогда
        ДанныеСтроки.ДатаОтгрузки = Результат; 
        ИзменитьДатуОтгрузкиНаСервере(ДанныеСтроки); 
        ОбновитьСписокПоТаймеру();
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаЦены(Результат, ДанныеСтроки) Экспорт
	Если Результат <> Неопределено Тогда
        ДанныеСтроки.Цена = Результат; 
        ИзменитьЦенуНаСервере(ДанныеСтроки); 
        Элементы.ДанныеАРМОтображение.Обновить();
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаКоэффициента(Результат, ДанныеСтроки) Экспорт
	Если Результат <> Неопределено Тогда
        ДанныеСтроки.Коэффициент = Результат; 
        ИзменитьКоэффициентНаСервере(ДанныеСтроки); 
        Элементы.ДанныеАРМОтображение.Обновить();
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
    КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ИзменитьЦенуНаСервере(ДанныеСтроки)
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	Запись = Набор[0];
	Запись.Цена = ДанныеСтроки.Цена;
	Если ДанныеСтроки.ВидОперации = Перечисления.Арм_ВидыОпераций.Продажа И ДанныеСтроки.СебестоимостьЕдиницы <> 0 Тогда 
		Запись.Коэффициент = ДанныеСтроки.Цена / ДанныеСтроки.СебестоимостьЕдиницы;
		Запись.ТипИзмененияЦены = 1;
	КонецЕсли;
	Попытка
		Набор.Записать();       
	Исключение
		ТекстОшибки = СтрШаблон(
			"Ошибка записи в АРМ-регистр. Цена: %1, Описание: %2", 
			Строка(ДанныеСтроки.ИдентификаторЗаписи), 
			КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
		);
		ЗаписьЖурналаРегистрации("АРМ.ЗаписьДанных", УровеньЖурналаРегистрации.Ошибка, ,	, ТекстОшибки);
	КонецПопытки;
КонецПроцедуры

&НаСервере
Процедура ИзменитьКоэффициентНаСервере(ДанныеСтроки)
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();
	Запись = Набор[0];
	Запись.Коэффициент = ДанныеСтроки.Коэффициент;
	Если ДанныеСтроки.ВидОперации = Перечисления.Арм_ВидыОпераций.Продажа И ДанныеСтроки.СебестоимостьЕдиницы <> 0 Тогда
		Запись.Цена = ДанныеСтроки.Коэффициент * ДанныеСтроки.СебестоимостьЕдиницы;
		Запись.ТипИзмененияЦены = 2;
	КонецЕсли;
	Попытка
		Набор.Записать();       
	Исключение
		ТекстОшибки = СтрШаблон(
			"Ошибка записи в АРМ-регистр. Коэффициент: %1, Описание: %2", 
			Строка(ДанныеСтроки.ИдентификаторЗаписи), 
			КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
		);
		ЗаписьЖурналаРегистрации("АРМ.ЗаписьДанных", УровеньЖурналаРегистрации.Ошибка, ,	, ТекстОшибки);
	КонецПопытки;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьКомментарийВРегистр(ИдентификаторЗаписи, ТекстКомментария)
    
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
    Набор.Отбор.ИдентификаторЗаписи.Установить(ИдентификаторЗаписи);
    Набор.Прочитать();
    
    Если Набор.Количество() > 0 Тогда
        
        Запись = Набор[0];
        Запись.КомментарийКСтроке = ТекстКомментария;
        
        Попытка
            Набор.Записать();
        Исключение
            ЗаписьЖурналаРегистрации("АРМ.ОшибкаЗаписиКомментария", 
                УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
        КонецПопытки;
        
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ИзменитьДатуОтгрузкиНаСервере(ДанныеСтроки)
    
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
    Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
    Набор.Прочитать();
    
    Если Набор.Количество() > 0 Тогда
        
        Запись = Набор[0];
        Запись.ДатаОтгрузки = ДанныеСтроки.ДатаОтгрузки;
        
        Попытка
            Набор.Записать();
        Исключение
            ЗаписьЖурналаРегистрации("АРМ.ОшибкаЗаписиДатаОтгрузки", 
                УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
        КонецПопытки;
        
    КонецЕсли;
    
КонецПроцедуры 

&НаСервере
Процедура ИзменитьНомерОтгрузкиНаСервере(ДанныеСтроки)
    
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
    Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
    Набор.Прочитать();
    
    Если Набор.Количество() > 0 Тогда
        
        Запись = Набор[0];
        Запись.НомерОтгрузки = ДанныеСтроки.НомерОтгрузки;
        
        Попытка
            Набор.Записать();
        Исключение
            ЗаписьЖурналаРегистрации("АРМ.ОшибкаЗаписиНомерОтгрузки", 
                УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
        КонецПопытки;
        
    КонецЕсли;
    
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СохранитьГРЗВРегистр(ИдентификаторЗаписи, ГРЗКлиентаТекст)
    
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
    Набор.Отбор.ИдентификаторЗаписи.Установить(ИдентификаторЗаписи);
    Набор.Прочитать();
    
    Если Набор.Количество() > 0 Тогда
        
        Запись = Набор[0];
        Запись.ГРЗКлиента = ГРЗКлиентаТекст;
        
        Попытка
            Набор.Записать();
        Исключение
            ЗаписьЖурналаРегистрации("АРМ.ОшибкаЗаписиКомментария", 
                УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
        КонецПопытки;
        
    КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ИзменитьКоличествоНаСервере(ДанныеСтроки)
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	
	// ПРАВКА №1 БРО! Жестко запираем СУБД по обоим ведущим измерениям регистра!
	// Теперь платформа 1С прочитает строго ТУ ОДНУ СТРОКУ, которую менеджер меняет на экране,
	// полностью исключив сбои кэша и ложное размножение строк-паразитов!
	Набор.Отбор.ДокументЗаказПокупателя.Установить(ДанныеСтроки.ДокументЗаказПокупателя);
	Набор.Отбор.ИдентификаторЗаписи.Установить(ДанныеСтроки.ИдентификаторЗаписи);
	Набор.Прочитать();

	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
	Иначе 
		Возврат;
	КонецЕсли;
	
	СтароеКоличество   = Запись.Количество;
	НовоеКоличество    = ДанныеСтроки.Количество;
	СтараяНоменклатура = Запись.Номенклатура;
	
	Если СтароеКоличество = НовоеКоличество Тогда
		Возврат;
	КонецЕсли;
	
	// -------------------------------------------------------------------------
	// КОНТУР ОБНУЛЕНИЯ СТРОК АРМА (ЕСЛИ ВВЕЛИ 0)
	
	Если НовоеКоличество = 0 И Запись.ВидОперации = Перечисления.Арм_ВидыОпераций.Закупка Тогда
		Набор.Очистить();
		Набор.Записать();
		Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(ДанныеСтроки.Номенклатура, ДанныеСтроки.ДокументЗаказПокупателя);
		Возврат; 
	КонецЕсли;

	Если НовоеКоличество = 0 И Запись.ВидОперации = Перечисления.Арм_ВидыОпераций.Продажа Тогда
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
			"ВЫБРАТЬ

			|	КОЛИЧЕСТВО(Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи) КАК СтрокВЗаказе
			|ИЗ
			|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж

			|ГДЕ
			|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя = &ДокументЗаказПокупателя";
		
		Запрос.УстановитьПараметр("ДокументЗаказПокупателя", ДанныеСтроки.ДокументЗаказПокупателя);
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаСтрокВЗаказе = РезультатЗапроса.Выбрать();
		Если ВыборкаСтрокВЗаказе.Следующий() Тогда
			Если ВыборкаСтрокВЗаказе.СтрокВЗаказе > 1 Тогда
				Набор.Очистить();
				Набор.Записать();
				Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(ДанныеСтроки.Номенклатура, ДанныеСтроки.ДокументЗаказПокупателя);
				Возврат;
			Иначе
				Возврат;
			КонецЕсли;
		Иначе
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	// -------------------------------------------------------------------------
	// КОНТУР ДРОБЛЕНИЯ СТРОК СНАБЖЕНИЯ ПРИ ИЗМЕНЕНИИ КОЛИЧЕСТВА
	
	Если ДанныеСтроки.ВидОперации = Перечисления.Арм_ВидыОпераций.Закупка Тогда
			
		Если Запись.РучнаяПравка = Истина Тогда
			Запись.Количество = НовоеКоличество;
		Иначе
			КоличествоДельты = 0;
			Если НовоеКоличество > СтароеКоличество Тогда
				
				// КЕЙС А: Увеличение количества (6 меняем на 10 штук)
				КоличествоДельты = НовоеКоличество - СтароеКоличество; // 10 - 6 = 4 шт
				
				// Рождаем изолированную верхушку запаса на 4 шт со своим уникальным GUID-ключом связи
				СоздатьРучнуюДельтуНаСервере(ДанныеСтроки, КоличествоДельты, Запись.НомерЗаказаКлиента);
				
				// ХИРУРГИЧЕСКИЙ ФИКС БРО! Убираем отсюда ложное Запись.РучнаяПравка = Истина!
				// Исходная строка (6 шт) остается чистокровным АВТО-ДЕФИЦИТОМ (РучнаяПравка = ЛОЖЬ),
				// а новая верхушка запаса (4 шт) ложится рядом как честная Ручная Правка!
				Запись.Количество = СтароеКоличество; // Честно возвращаем исходные 6 шт авто-дефицита
				
				// Возвращаем визуал на форме для синхронизации интерфейса
				ДанныеСтроки.Количество = СтароеКоличество; 
				
			ИначеЕсли НовоеКоличество < СтароеКоличество Тогда
				
				// КЕЙС Б: Уменьшение количества (ИЗОЛЯЦИЯ УРЕЗАННОЙ ЗАКУПКИ ПО НОВОМУ КЛЮЧУ)
				КоличествоДельты = СтароеКоличество - НовоеКоличество; // 5 - 3 = 2 шт
				
				// 1. Исходная строка закупки (3 шт) становится ручной правкой 
				// и получает АБСОЛЮТНО НОВЫЙ уникальный ключ связи, полностью изолируясь от авто-робота!
				Запись.Количество          = НовоеКоличество;
				Запись.РучнаяПравка        = Истина;
				Запись.КлючСвязиСтрок      = Строка(Новый УникальныйИдентификатор);
				Запись.НомерЗаказаКлиента = ДанныеСтроки.НомерЗаказаКлиента;
				
				// Фиксируем урезание в параметрах клиента для синхронизации интерфейса
				ДанныеСтроки.Количество = НовоеКоличество; 
				
				// 2. Рождаем новую авто-Закупку на остаток дефицита (2 шт)
				// ВНИМАНИЕ БРО: Мы оставляем ей СТАРЫЙ исторический ключ связи продаж,
				// чтобы FIFO-монстр мог правильно обсчитать под нее дефицит и ОстатокДляСтроки!
				НоваяЗапись = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьМенеджерЗаписи();
		        ЗаполнитьЗначенияСвойств(НоваяЗапись, ДанныеСтроки);
		        
		        НоваяЗапись.ИдентификаторЗаписи = Строка(Новый УникальныйИдентификатор());
		        НоваяЗапись.КлючСвязиСтрок      = ДанныеСтроки.КлючСвязиСтрок; // Старый ключ продаж!
		        НоваяЗапись.НомерЗаказаКлиента = Запись.НомерЗаказаКлиента;
		        
		        НоваяЗапись.Количество          = КоличествоДельты; // Получит честные 2 шт дефицита
		        НоваяЗапись.РучнаяПравка        = Ложь; // Она остается в авто-контуре!
		        НоваяЗапись.Порядок             = ДанныеСтроки.Порядок + 0.01;
				НоваяЗапись.Цена				= 0;
				
				Попытка
		        	НоваяЗапись.Записать();
				Исключение
					ТекстОшибки = СтрШаблон("Ошибка записи сплит-строки АРМ: %1", КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
					ЗаписьЖурналаРегистрации("АРМ.ЗаписьДанных", УровеньЖурналаРегистрации.Ошибка, , , ТекстОшибки);
				КонецПопытки;
				
			КонецЕсли;
		КонецЕсли;
		
		Попытка
			Набор.Записать();
			Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(ДанныеСтроки.Номенклатура, ДанныеСтроки.ДокументЗаказПокупателя);
		Исключение
			ТекстОшибки = СтрШаблон("Ошибка записи набора закупки АРМ: %1", КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			ЗаписьЖурналаРегистрации("АРМ.ЗаписьДанных", УровеньЖурналаРегистрации.Ошибка, , , ТекстОшибки);
		КонецПопытки;
		
	Иначе
		
		// Контур Сбыта (Продажи) — пишем изменения количества «как есть»
		Запись.Количество = ДанныеСтроки.Количество;
		
		// ПРАВКА №5: На всякий случай страхуем Номер Заказа Клиента в продажах при перезаписи
		Запись.НомерЗаказаКлиента = ДанныеСтроки.НомерЗаказаКлиента;
		
		Попытка
			Набор.Записать();
			Если ЗначениеЗаполнено(Запись.Номенклатура) Тогда
				Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(ДанныеСтроки.Номенклатура, ДанныеСтроки.ДокументЗаказПокупателя);
			КонецЕсли;
		Исключение
			ТекстОшибки = СтрШаблон("Ошибка записи набора продаж АРМ: %1", КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
			ЗаписьЖурналаРегистрации("АРМ.ЗаписьДанных", УровеньЖурналаРегистрации.Ошибка, , , ТекстОшибки);
		КонецПопытки;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВыбораЗначенияНоменклатура(РезультатВыбора, ДополнительныеПараметры) Экспорт
    
    Если РезультатВыбора <> Неопределено Тогда
		ЗаписатьЗначениеВРегистрНаСервереНоменклатура(
			ДополнительныеПараметры.ИдентификаторЗаписи, 
			ДополнительныеПараметры.ИмяПоля, 
			РезультатВыбора
		);
		Элементы.ДанныеАРМОтображение.Обновить();
		Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
	КонецЕсли;
    
КонецПроцедуры

&НаСервере
Процедура ЗаписатьЗначениеВРегистрНаСервереНоменклатура(Идентификатор, ИмяПоля, НоменклатураНовоеЗначение)
	
	СтараяНоменклатура = Справочники.Номенклатура.ПустаяСсылка();
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ИдентификаторЗаписи.Установить(Идентификатор);
	Набор.Прочитать();

	Если Набор.Количество() > 0 Тогда
	    СтараяНоменклатура = Набор[0].Номенклатура;
		СсылкаНаЗаказ = Набор[0].ДокументЗаказПокупателя; 
	КонецЕсли;
	
	НоменклатураИзменена = (СтараяНоменклатура <> НоменклатураНовоеЗначение);
	
	Если Не НоменклатураИзменена Тогда
		Возврат;
	КонецЕсли;
	
    Если Набор.Количество() > 0 Тогда
        Запись = Набор[0];
		
		Если Запись.ВидОперации = Перечисления.Арм_ВидыОпераций.Продажа Тогда
			Запись.КлючСвязиСтрок = Строка(Новый УникальныйИдентификатор());
			Запись.Партия = Справочники.ПартииНоменклатуры.ПустаяСсылка();
		КонецЕсли;

        Запись[ИмяПоля] = НоменклатураНовоеЗначение;
        Запись.Порядок  = Арм_ОбщегоНазначенияАРМ.ПолучитьПриоритет(НоменклатураНовоеЗначение);
		Запись.ОстатокДляСтроки = 0; 

        Набор.Записать();
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтараяНоменклатура) Тогда
		Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(СтараяНоменклатура, СсылкаНаЗаказ);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(НоменклатураНовоеЗначение) Тогда
		Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(НоменклатураНовоеЗначение, СсылкаНаЗаказ);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура УдалитьСтроку(Команда)
	ВычеркнутьСтрокуНаСервере();
	Элементы.ДанныеАРМОтображение.Обновить();
	Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьДокументЗаказПокупателя(Команда)
	
	СписокДокументов = ПолучитьСписокНужныхДокументовДляОткрытия("ДокументЗаказПокупателя");
    Для Каждого ДокументСсылка Из СписокДокументов Цикл
        ПоказатьЗначение(, ДокументСсылка);
    КонецЦикла;
	Если СписокДокументов.Количество() = 0 Тогда
		ТекстСообщения = "Для выделенных строк отсутствует документ заказ покупателя";
		ПоказатьОповещениеПользователя("Не данных для отображения", , ТекстСообщения);
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьДокументЗаказПоставщику(Команда)
	СписокДокументов = ПолучитьСписокНужныхДокументовДляОткрытия("ДокументЗаказПоставщика");
    Для Каждого ДокументСсылка Из СписокДокументов Цикл
        ПоказатьЗначение(, ДокументСсылка);
	КонецЦикла;
	Если СписокДокументов.Количество() = 0 Тогда
		ТекстСообщения = "Для выделенных строк отсутствует документ заказ поставщику";
		ПоказатьОповещениеПользователя("Не данных для отображения", , ТекстСообщения);
	КонецЕсли;	
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьДокументПриходнаяНакладная(Команда)
	СписокДокументов = ПолучитьСписокНужныхДокументовДляОткрытия("ДокументПриходнаяНакладная");
    Для Каждого ДокументСсылка Из СписокДокументов Цикл
        ПоказатьЗначение(, ДокументСсылка);
	КонецЦикла;
	Если СписокДокументов.Количество() = 0 Тогда
		ТекстСообщения = "Для выделенных строк отсутствует документ приходная накладная";
		ПоказатьОповещениеПользователя("Не данных для отображения", , ТекстСообщения);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьВсеДокументы(Команда)
	
	СписокДокументов = ПолучитьСписокДокументовДляОткрытия();
    Для Каждого ДокументСсылка Из СписокДокументов Цикл
        ПоказатьЗначение(, ДокументСсылка);
    КонецЦикла;
		
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьСписокДокументовДляОткрытия()
	
	Запрос = Новый Запрос;
    Запрос.Текст = 
        "ВЫБРАТЬ РАЗЛИЧНЫЕ
        |	Т.Ссылка КАК Ссылка
        |ИЗ
        |	(ВЫБРАТЬ
        |		Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя КАК Ссылка
        |	ИЗ
        |		РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
        |	ГДЕ
        |		Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
        |		И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &Автор
        |		И Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя <> &ПустойЗаказПокупателя
        |	
        |	ОБЪЕДИНИТЬ ВСЕ
        |	
        |	ВЫБРАТЬ
        |		Арм_ДанныеЗакупокИПродаж.ДокументЗаказПоставщика
        |	ИЗ
        |		РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
        |	ГДЕ
        |		Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
        |		И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &Автор
        |		И Арм_ДанныеЗакупокИПродаж.ДокументЗаказПоставщика <> &ПустойЗаказПоставщика
		|
		|	ОБЪЕДИНИТЬ ВСЕ
        |	
        |	ВЫБРАТЬ
        |		Арм_ДанныеЗакупокИПродаж.РасходнаяНакладная
        |	ИЗ
        |		РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
        |	ГДЕ
        |		Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
        |		И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &Автор
        |		И Арм_ДанныеЗакупокИПродаж.РасходнаяНакладная <> &ПустаяРасходнаяНакладная
        |	
        |	ОБЪЕДИНИТЬ ВСЕ
        |	
        |	ВЫБРАТЬ
        |		Арм_ДанныеЗакупокИПродаж.ДокументПриходнаяНакладная
        |	ИЗ
        |		РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
        |	ГДЕ
        |		Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
        |		И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &Автор
        |		И Арм_ДанныеЗакупокИПродаж.ДокументПриходнаяНакладная <> &ПустаяПриходнаяНакладная) КАК Т";
    
    Запрос.УстановитьПараметр("Автор", ПараметрыСеанса.ТекущийПользователь);
    
    Запрос.УстановитьПараметр("ПустойЗаказПокупателя",		Документы.ЗаказПокупателя.ПустаяСсылка());
    Запрос.УстановитьПараметр("ПустойЗаказПоставщика", 		Документы.ЗаказПоставщику.ПустаяСсылка());
    Запрос.УстановитьПараметр("ПустаяПриходнаяНакладная",   Документы.ПриходнаяНакладная.ПустаяСсылка());
	Запрос.УстановитьПараметр("ПустаяРасходнаяНакладная",   Документы.РасходнаяНакладная.ПустаяСсылка());
	
    Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
Конецфункции

&НаСервереБезКонтекста
Функция ПолучитьСписокНужныхДокументовДляОткрытия(НужныйДокумент)
	
	Запрос = Новый Запрос;
    Запрос.Текст = 
        "ВЫБРАТЬ РАЗЛИЧНЫЕ
        |	Т.Ссылка КАК Ссылка
        |ИЗ
        |	(ВЫБРАТЬ
        |		Арм_ДанныеЗакупокИПродаж.НужныйДокумент КАК Ссылка
        |	ИЗ
        |		РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
        |	ГДЕ
        |		Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
        |		И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &Автор
        |		И Арм_ДанныеЗакупокИПродаж.НужныйДокумент <> &ПустойДокумент) КАК Т";
	
	Запрос.Текст = СтрЗаменить(
		Запрос.Текст, 
		"Арм_ДанныеЗакупокИПродаж.НужныйДокумент", 
		"Арм_ДанныеЗакупокИПродаж." + НужныйДокумент
	);	
    Запрос.УстановитьПараметр("Автор", ПараметрыСеанса.ТекущийПользователь);
	
	Если НужныйДокумент = "ДокументЗаказПокупателя" Тогда
    	Запрос.УстановитьПараметр("ПустойДокумент", Документы.ЗаказПокупателя.ПустаяСсылка());
	ИначеЕсли НужныйДокумент = "ДокументЗаказПоставщика" Тогда
		Запрос.УстановитьПараметр("ПустойДокумент", Документы.ЗаказПоставщику.ПустаяСсылка());
	ИначеЕсли НужныйДокумент = "ДокументПриходнаяНакладная" Тогда
		Запрос.УстановитьПараметр("ПустойДокумент", Документы.ПриходнаяНакладная.ПустаяСсылка());
	Иначе
		Запрос.УстановитьПараметр("ПустойДокумент", Документы.РасходнаяНакладная.ПустаяСсылка());
	КонецЕсли;
    
    Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
Конецфункции

&НаСервереБезКонтекста
Процедура ВычеркнутьСтрокуНаСервере()
	
	СписокДляПересчета = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Арм_ДанныеЗакупокИПродаж.КлючСвязиСтрок КАК КлючСвязиСтрок
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_Отмеченные
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|		ПО Арм_Отмеченные.КлючСвязиСтрок = Арм_ДанныеЗакупокИПродаж.КлючСвязиСтрок
		|ГДЕ
		|	Арм_Отмеченные.ОтметкаСтроки
		|	И Арм_Отмеченные.АвторСтроки = &АвторСтроки
		|	И Арм_Отмеченные.ВидОперации В(&ВидОперации)
		|	И Арм_Отмеченные.СтатусСтроки В(&СтатусыСтрок)";
	
	Запрос.УстановитьПараметр("АвторСтроки", ПараметрыСеанса.ТекущийПользователь);
	МассивВидовОперации = Новый Массив;
	МассивВидовОперации.Добавить(Перечисления.Арм_ВидыОпераций.Продажа);
	МассивВидовОперации.Добавить(Перечисления.Арм_ВидыОпераций.Закупка);
	Запрос.УстановитьПараметр("ВидОперации", МассивВидовОперации);
	МассивСтатусыСтрок = Новый Массив;
	МассивСтатусыСтрок.Добавить(Перечисления.Арм_СтатусыАРМ.Черновик);
	МассивСтатусыСтрок.Добавить(Перечисления.Арм_СтатусыАРМ.ВРаботе);
	Запрос.УстановитьПараметр("СтатусыСтрок", МассивСтатусыСтрок);
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаОтмеченныхСтрок = РезультатЗапроса.Выбрать();
	
	Пока ВыборкаОтмеченныхСтрок.Следующий() Цикл
		
		Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	    Набор.Отбор.КлючСвязиСтрок.Установить(ВыборкаОтмеченныхСтрок.КлючСвязиСтрок);
	    Набор.Прочитать();
		
		Если Набор.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;

		НовоеСостояниеУдаления = Не Набор[0].Удаление; 
		
		Для Каждого Запись Из Набор Цикл
			
			Если Запись.РучнаяПравка Тогда
				Продолжить;
			КонецЕсли;
			
			Если ЗначениеЗаполнено(Запись.Номенклатура) Тогда
				
				Запись.Удаление = НовоеСостояниеУдаления; 
				Запись.ОтметкаСтроки = Ложь;
				Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка();
				Если НовоеСостояниеУдаления = Ложь Тогда
					Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.ВРаботе;
				Иначе
					Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Черновик;
				КонецЕсли;
				СтруктураКлюч = Новый Структура("Номенклатура, Заказ", Запись.Номенклатура, Запись.ДокументЗаказПокупателя);
				СписокДляПересчета.Вставить(СтруктураКлюч, Истина);
				
			КонецЕсли;
		КонецЦикла;
		
		Попытка
			Набор.Записать();
		Исключение
			ЗаписьЖурналаРегистрации("АРМ.Вычеркивание", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки());
		КонецПопытки;
		
	КонецЦикла;
	
	Для Каждого КлючИЗначение Из СписокДляПересчета Цикл
        Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(КлючИЗначение.Ключ.Номенклатура, КлючИЗначение.Ключ.Заказ);
    КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьИдентификаторНоменклатуры()
	Возврат Элементы.ИдентификатoрНоменклатуры.СписокВыбора.ВыгрузитьЗначения();
КонецФункции

&НаСервере
Процедура СоздатьРучнуюДельтуНаСервере(ДанныеСтроки, КоличествоДельты, НомерЗаказаКлиента)
    
	//Менеджер = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьМенеджерЗаписи();
	//
	//ЗаполнитьЗначенияСвойств(Менеджер, ДанныеСтроки);
	//
	//Менеджер.ИдентификаторЗаписи = Строка(Новый УникальныйИдентификатор());
	//Менеджер.ВидОперации         = ПредопределенноеЗначение("Перечисление.Арм_ВидыОпераций.Закупка");
	//Менеджер.РучнаяПравка        = Истина;    
	//Менеджер.Количество          = КоличествоДельты;
	//Менеджер.Порядок             = ДанныеСтроки.Порядок + 0.01;
	//Менеджер.Цена = 0;
	//
	//Попытка
	//    Менеджер.Записать();
	//Исключение
	//    ЗаписьЖурналаРегистрации("АРМ.РучнаяПравка", УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
	//КонецПопытки;
	
	
	
	
	
	
	 Менеджер = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьМенеджерЗаписи();
    
    ЗаполнитьЗначенияСвойств(Менеджер, ДанныеСтроки);
	
    // ПРАВКА-ТИТАН БРО! Намертво изолируем строку запаса от старой истории!
    // Генерируем ей СВЕЖИЙ ИдентификаторЗаписи и СВЕЖИЙ Ключ Связи Строк!
    Менеджер.ИдентификаторЗаписи = Строка(Новый УникальныйИдентификатор);
    Менеджер.КлючСвязиСтрок      = Строка(Новый УникальныйИдентификатор);
    
    // НАДЁЖНО ЗАКРЕПЛЯЕМ СКВОЗНОЙ НОМЕР ЗАКАЗА ДЛЯ НАШЕЙ НОВОЙ ВЕРХУШКИ ЗАПАСА!
    Менеджер.НомерЗаказаКлиента  = НомерЗаказаКлиента;
    
    Менеджер.ВидОперации         = ПредопределенноеЗначение("Перечисление.Арм_ВидыОпераций.Закупка");
    Менеджер.РучнаяПравка        = Истина;    
    Менеджер.Количество          = КоличествоДельты;
    Менеджер.Порядок             = ДанныеСтроки.Порядок + 0.01;
	Менеджер.Цена                = 0;
    
    Попытка
        Менеджер.Записать();
    Исключение
        ЗаписьЖурналаРегистрации("АРМ.РучнаяПравка", УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
    КонецПопытки;
    
КонецПроцедуры

&НаСервере
Процедура СоздатьЗаказыПоставщикамНаСервере_________2(ТЗ_Данные)
	
	ТЗ_Поставщики = ТЗ_Данные.Скопировать();
	ТЗ_Поставщики.Свернуть("Поставщик, Договор, ДокументЗаказПокупателя");
	
	Для Каждого СтрПоставщик Из ТЗ_Поставщики Цикл
		
		Если Не ЗначениеЗаполнено(СтрПоставщик.Поставщик) Тогда 
			Продолжить; 
		КонецЕсли;
		
		ЗаказОбъект = Документы.ЗаказПоставщику.СоздатьДокумент();
		ЗаказОбъект.Заполнить(Неопределено);
		ЗаказОбъект.Дата = ТекущаяДатаСеанса();
		ЗаказОбъект.Контрагент = СтрПоставщик.Поставщик;
		
		СостояниеЗаказа = Справочники.СостоянияЗаказовПоставщикам.НайтиПоНаименованию("В работе");
		Если СостояниеЗаказа = Неопределено Тогда
			СостояниеЗаказа = Справочники.СостоянияЗаказовПоставщикам.СоздатьЭлемент();
			СостояниеЗаказа.Наименование = "В работе";
			СостояниеЗаказа.Записать();	
		КонецЕсли;
		
		ЗаказОбъект.СостояниеЗаказа = СостояниеЗаказа.Ссылка;
		
		Если ЗначениеЗаполнено(СтрПоставщик.Договор) Тогда
			ДоговорПоставщика = СтрПоставщик.Договор;
		Иначе
			Запрос = Новый Запрос;
			Запрос.Текст = 
				"ВЫБРАТЬ
				|	ДоговорыКонтрагентов.Ссылка КАК Ссылка
				|ИЗ
				|	Справочник.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
				|ГДЕ
				|	НЕ ДоговорыКонтрагентов.ПометкаУдаления
				|	И ДоговорыКонтрагентов.Владелец.Ссылка = &Ссылка
				|	И НЕ ДоговорыКонтрагентов.Владелец.Недействителен
				|	И ДоговорыКонтрагентов.ДатаДоговора <= &ТекущаяДата
				|	И ДоговорыКонтрагентов.СрокДействия >= &ТекущаяДата
				|
				|ОБЪЕДИНИТЬ ВСЕ
				|
				|ВЫБРАТЬ
				|	ДоговорыКонтрагентов.Ссылка
				|ИЗ
				|	Справочник.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
				|ГДЕ
				|	НЕ ДоговорыКонтрагентов.ПометкаУдаления
				|	И ДоговорыКонтрагентов.Владелец.Ссылка = &Ссылка
				|	И НЕ ДоговорыКонтрагентов.Владелец.Недействителен
				|	И ДоговорыКонтрагентов.СрокДействия = ДАТАВРЕМЯ(1, 1, 1)";
			
			Запрос.УстановитьПараметр("Ссылка", СтрПоставщик.Поставщик);
			Запрос.УстановитьПараметр("ТекущаяДата", ТекущаяДатаСеанса());
			РезультатЗапроса = Запрос.Выполнить();
			ВыборкаДоговор = РезультатЗапроса.Выбрать();
			
			Если ВыборкаДоговор.Следующий() Тогда
				ДоговорПоставщика = ВыборкаДоговор.Ссылка;
			КонецЕсли;
		КонецЕсли;
		
		ЗаказОбъект.Договор = ДоговорПоставщика;		
		ЗаказОбъект.ДатаПоступления = ТекущаяДатаСеанса();
	    ЗаказОбъект.ДокументОснование = СтрПоставщик.ДокументЗаказПокупателя;
		ЗаказОбъект.ЗаказПокупателя = СтрПоставщик.ДокументЗаказПокупателя;
		
		СтрокиДляЗаказа = ТЗ_Данные.НайтиСтроки(Новый Структура("Поставщик", СтрПоставщик.Поставщик));
		
		Запрос = Новый Запрос;
		Запрос.Текст = 
			"ВЫБРАТЬ РАЗЛИЧНЫЕ
			|	СтавкиНДС.Ссылка КАК Ссылка
			|ИЗ
			|	Справочник.СтавкиНДС КАК СтавкиНДС
			|ГДЕ
			|	СтавкиНДС.Наименование = &Наименование
			|	И НЕ СтавкиНДС.ПометкаУдаления
			|
			|ОБЪЕДИНИТЬ ВСЕ
			|
			|ВЫБРАТЬ РАЗЛИЧНЫЕ
			|	СтавкиНДС.Ссылка
			|ИЗ
			|	Справочник.СтавкиНДС КАК СтавкиНДС
			|ГДЕ
			|	СтавкиНДС.Ставка = &РазмерСтавки
			|	И НЕ СтавкиНДС.ПометкаУдаления";
		
		Запрос.УстановитьПараметр("Наименование", "20%");
		Запрос.УстановитьПараметр("РазмерСтавки", 20);
		РезультатЗапросаСтавка = Запрос.Выполнить();
		ВыборкаСтавкаНДС = РезультатЗапросаСтавка.Выбрать();
		
		Если ВыборкаСтавкаНДС.Следующий() Тогда
			СтавкаНДС = ВыборкаСтавкаНДС.Ссылка; 
		КонецЕсли;
		
		Для Каждого Стр Из СтрокиДляЗаказа Цикл
			
			НоваяСтр = ЗаказОбъект.Запасы.Добавить();
			НоваяСтр.Номенклатура = Стр.Номенклатура;
			НоваяСтр.Количество = Стр.Количество;
			НоваяСтр.Цена = Стр.Цена;
			НоваяСтр.Сумма = Стр.Количество * Стр.Цена;
			НоваяСтр.СтавкаНДС = СтавкаНДС;

			ПараметрыРасчета = Новый Структура;
			ПараметрыРасчета.Вставить("СуммаВключаетНДС", ЗаказОбъект.СуммаВключаетНДС);
			ТабличныеЧастиУНФКлиентСервер.РассчитатьСуммуНДСИВсего(НоваяСтр, ПараметрыРасчета) ;
			
		КонецЦикла;
		
		ЗаказОбъект.Записать(РежимЗаписиДокумента.Проведение);
		ОбновитьСсылкуВРегистре(СтрокиДляЗаказа, ЗаказОбъект.Ссылка);
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ОбновитьСсылкуВРегистре(СтрокиТЗ, СсылкаНаЗаказ = Неопределено)
	Для Каждого Стр Из СтрокиТЗ Цикл
		Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
		Набор.Отбор.ИдентификаторЗаписи.Установить(Стр.ИдентификаторЗаписи);
		Набор.Прочитать();
		Если Набор.Количество() > 0 Тогда
			//Набор[0].СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Заказано;
			Если ЗначениеЗаполнено(СсылкаНаЗаказ) Тогда
				Набор[0].ДокументЗаказПоставщика = СсылкаНаЗаказ;
			КонецЕсли;
			Набор.Записать(); 
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура СоздатьЗаказПокупателя(Команда)
	ОткрытьФорму("Документ.ЗаказПокупателя.ФормаОбъекта");
КонецПроцедуры

&НаКлиенте
Процедура Подготовлено(Команда)
	ПодготовленоНаСервере();
	Элементы.ДанныеАРМОтображение.Обновить();
	Элементы.ДанныеАРМОтображениеЗакупки.Обновить();
КонецПроцедуры

&НаСервере
Процедура ПодготовленоНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст =  
		"ВЫБРАТЬ
		|	Т_Закупка.КлючСвязиСтрок КАК КлючСвязиСтрок
		|ПОМЕСТИТЬ втНезавершенныеЗакупки
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т_Закупка
		|ГДЕ
		|	Т_Закупка.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Закупка)
		|	И Т_Закупка.СтатусСтроки <> ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.НаСкладе)
		|	И НЕ Т_Закупка.Удаление
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	Т.ИдентификаторЗаписи КАК ИдентификаторЗаписи
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.ОтметкаСтроки = ИСТИНА
		|	И Т.АвторСтроки = &АвторСтроки
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Т.Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
		|	И Т.Поставщик <> ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка)
		|	И Т.Количество <> 0
		|	И Т.Цена <> 0
		|	И Т.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.ВРаботе)
		|	И НЕ Т.Удаление
		|	И НЕ Т.КлючСвязиСтрок В
		|				(ВЫБРАТЬ
		|					втНезавершенныеЗакупки.КлючСвязиСтрок КАК КлючСвязиСтрок
		|				ИЗ
		|					втНезавершенныеЗакупки КАК втНезавершенныеЗакупки)";
	
	Запрос.УстановитьПараметр("АвторСтроки", ПараметрыСеанса.ТекущийПользователь);
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю("В строках есть незавершенные закупки");
		Возврат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Пока Выборка.Следующий() Цикл
		
		Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
		Набор.Отбор.ИдентификаторЗаписи.Установить(Выборка.ИдентификаторЗаписи);
		Набор.Прочитать();
		
		Если Набор.Количество() > 0 Тогда
			
			Запись = Набор[0]; 
			Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Подготовлено;
			Запись.ОтметкаСтроки = Ложь;
			Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка();
			
			Попытка
				Набор.Записать();
			Исключение
				ЗаписьЖурналаРегистрации("АРМ.Подготовлено к продаже", УровеньЖурналаРегистрации.Ошибка, , , ОписаниеОшибки());
			КонецПопытки;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ПровестиЗаказПокупателя(Команда)
	ПровестиЗаказПокупателяНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПровестиЗаказПокупателяНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Т.ИдентификаторЗаписи КАК ИдентификаторЗаписи,
		|	Т.ДокументЗаказПокупателя КАК Заказ,
		|	Т.Покупатель КАК Покупатель,
		|	Т.Договор КАК Договор
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.ОтметкаСтроки = ИСТИНА
		|	И Т.АвторСтроки = &АвторСтроки
		|	И Т.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Т.Номенклатура <> ЗНАЧЕНИЕ(Справочник.Номенклатура.ПустаяСсылка)
		|	И Т.Количество <> 0
		|	И Т.Цена <> 0
		|	И Т.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.Подготовлено)
		|	И НЕ Т.Удаление
		|ИТОГИ
		|	МАКСИМУМ(Покупатель),
		|	МАКСИМУМ(Договор)
		|ПО
		|	Заказ";
	
	Запрос.УстановитьПараметр("АвторСтроки", ПараметрыСеанса.ТекущийПользователь);
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаЗаказ = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаЗаказ.Следующий() Цикл
		
		Если ВыборкаЗаказ.Заказ.Пустая() Тогда 
			Продолжить; 
		КонецЕсли;
		
		ИмяДокумента = СокрЛП(Строка(ВыборкаЗаказ.Заказ));
		
		Если Не ЗначениеЗаполнено(ВыборкаЗаказ.Покупатель) Тогда
			ТекстСообщения = СтрШаблон("В документе %1 не заполнен покупатель", ИмяДокумента); 
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения); 
			Возврат;
		Иначе
			Покупатель = ВыборкаЗаказ.Покупатель;
		КонецЕсли;
		
		ДоговорПокупателя = ВыборкаЗаказ.Договор;
		Если Не ЗначениеЗаполнено(ДоговорПокупателя) Тогда
			ДоговорПокупателя = Арм_ОбщегоНазначенияАРМ.НайтиДоговор(Покупатель);
		КонецЕсли;
		Если Не ЗначениеЗаполнено(ДоговорПокупателя) Тогда
			ТекстСообщения = СтрШаблон("В документе %1 не заполнен договор", ИмяДокумента); 
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения); 
			Возврат;	
		КонецЕсли;
		
		ОбъектЗаказа = ВыборкаЗаказ.Заказ.ПолучитьОбъект();
        ОбъектЗаказа.Контрагент = Покупатель;
		ОбъектЗаказа.Договор = ДоговорПокупателя;
		
		ОбъектЗаказа.Запасы.Очистить(); 
		
		ВыборкаДетали = ВыборкаЗаказ.Выбрать();
		Пока ВыборкаДетали.Следующий() Цикл
			
			НоваяСтрока = ОбъектЗаказа.Запасы.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ВыборкаДетали);
			
			Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
			Набор.Отбор.ИдентификаторЗаписи.Установить(ВыборкаДетали.ИдентификаторЗаписи);
			Набор.Прочитать();
			
			Если Набор.Количество() > 0 Тогда
				
				Запись = Набор[0]; 
				Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Завершено;
				Запись.ОтметкаСтроки = Ложь;
				Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка();
				
				Попытка
					Набор.Записать();
				Исключение
					ЗаписьЖурналаРегистрации(
						"АРМ.Проведение Заказа покупателя", 
						УровеньЖурналаРегистрации.Ошибка, 
						, 
						, 
						ОписаниеОшибки()
					);
				КонецПопытки;
				
			КонецЕсли;
			
		КонецЦикла;
		
		Попытка
			ОбъектЗаказа.Записать(РежимЗаписиДокумента.Проведение);
		Исключение
			Сообщить("Не удалось провести " + СокрЛП(Строка(ОбъектЗаказа)) + ": " + ОписаниеОшибки());
		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура СгруппироватьПоЗаказуПокупателяПриИзменении(Элемент)
	
	Если СгруппироватьПоЗаказуПокупателя Тогда
		ГруппироватьПоНомеруЗаказаКлиента = Ложь;
	КонецЕсли;
	
	УстановитьГруппировкуАРМНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура УстановитьГруппировкуПоЗаказуПокупателяНаСервере()
	
	//Настройки = ДанныеАРМОтображение.КомпоновщикНастроек.Настройки;
	//Настройки.Структура.Очистить();
	//
	//Если СгруппироватьПоЗаказуПокупателя Тогда
	//    ГруппировкаПоЗаказу = Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
	//    
	//    ПолеГрупп = ГруппировкаПоЗаказу.ПоляГруппировки.Элементы.Добавить(Тип("ПолеГруппировкиКомпоновкиДанных"));
	//    ПолеГрупп.Поле = Новый ПолеКомпоновкиДанных("ДокументЗаказПокупателя");
	//    ПолеГрупп.Использование = Истина;
	//    
	//    Детальные = ГруппировкаПоЗаказу.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
	//	
	//	ГруппироватьПоНомеруЗаказаКлиента = Ложь;
	//Иначе
	//    Детальные = Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
	//КонецЕсли;
	//
	//Детальные.Использование = Истина;
	//Детальные.Выбор.Элементы.Добавить(Тип("АвтоВыбранноеПолеКомпоновкиДанных"));
	//
	//ДанныеАРМОтображение.КомпоновщикНастроек.ЗагрузитьНастройки(Настройки);
	//ХранилищеОбщихНастроек.Сохранить("АРМ_НастройкаГруппировки", "ФлагГруппировки", СгруппироватьПоЗаказуПокупателя);
	
КонецПроцедуры

&НаСервере
Процедура УстановитьПериодНаСервере()
	
	ПараметрыСписка = ДанныеАРМОтображение.КомпоновщикНастроек.Настройки.ПараметрыДанных;
	
	ПараметрНачало = ПараметрыСписка.Элементы.Найти("ДатаНачала");
	Если ПараметрНачало <> Неопределено Тогда
		ПараметрНачало.Значение      = Период.ДатаНачала;
		ПараметрНачало.Использование = ЗначениеЗаполнено(Период.ДатаНачала);
	КонецЕсли;
	
	ПараметрКонец = ПараметрыСписка.Элементы.Найти("ДатаОкончания");
	Если ПараметрКонец <> Неопределено Тогда
		ПараметрКонец.Значение      = КонецДня(Период.ДатаОкончания);
		ПараметрКонец.Использование = ЗначениеЗаполнено(Период.ДатаОкончания);
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьНовыеДанныеОтПокупателя(Команда)
	ПараметрыФормы = Новый Структура; 
	ОткрытьФорму("Обработка.АС_АРМ2.Форма.ФормаЗагрузкиДанных", ПараметрыФормы, ЭтаФорма);
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "ОбновитьСписокАРМ" Тогда
		//ОбновитьСписокПоТаймеру();
		
		ПолеОтбора = Новый ПолеКомпоновкиДанных("ДокументЗаказПокупателя");
		
		ГруппаОтбора = ДанныеАРМОтображение.КомпоновщикНастроек.ФиксированныеНастройки.Отбор;
		
		Для Каждого Элемент Из ГруппаОтбора.Элементы Цикл
			Если ТипЗнч(Элемент) = Тип("ЭлементОтбораКомпоновкиДанных") 
				И Элемент.ЛевоеЗначение = ПолеОтбора Тогда
				ГруппаОтбора.Элементы.Удалить(Элемент);
			КонецЕсли;
		КонецЦикла;
		
		НовыйОтбор = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		НовыйОтбор.ЛевоеЗначение  = ПолеОтбора;
		НовыйОтбор.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		НовыйОтбор.ПравоеЗначение = Параметр;
		НовыйОтбор.Использование  = Истина;
		
		ОбновитьСписокПоТаймеру();
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НеПоказыватьВычеркнутыеПозицииПриИзменении(Элемент)
	
	СкрытьПоказатьВычеркнутыеПозицииНаСервере();
	
КонецПроцедуры 

&НаСервере
Процедура СкрытьПоказатьВычеркнутыеПозицииНаСервере()
	
	ХранилищеОбщихНастроек.Сохранить("АРМ_НастройкиДанных", "НеПоказыватьВычеркнутые", НеПоказыватьВычеркнутыеПозиции);
    ПолеОтбора = Новый ПолеКомпоновкиДанных("Удаление");
    
    Для Каждого ЭлементОтбора Из ДанныеАРМОтображение.Отбор.Элементы Цикл
        Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных") 
            И ЭлементОтбора.ЛевоеЗначение = ПолеОтбора Тогда
            ДанныеАРМОтображение.Отбор.Элементы.Удалить(ЭлементОтбора);
        КонецЕсли;
    КонецЦикла;

    Если НеПоказыватьВычеркнутыеПозиции Тогда
        НовыйОтбор = ДанныеАРМОтображение.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
        НовыйОтбор.ЛевоеЗначение  = ПолеОтбора;
        НовыйОтбор.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
        НовыйОтбор.ПравоеЗначение = Ложь;
        НовыйОтбор.Использование  = Истина;
        НовыйОтбор.РежимОтображения = РежимОтображенияЭлементаНастройкиКомпоновкиДанных.Недоступный;
    КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОчиститьПериод(Команда)
	
	Период = Неопределено;
	УстановитьПериодНаСервере();
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура СброситьФлагиНаСервере()
	Пользователь = ПараметрыСеанса.ТекущийПользователь;
	РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СброситьФлагиПользователя(Пользователь);
КонецПроцедуры

&НаКлиенте
Процедура ДанныеАРМОтображениеЗакупкиПриАктивизацииСтроки(Элемент)
	
	ОтображениеКнопокДокументов(Элемент);
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ТекущиеДанные.Свойство("ОтметкаСтроки") И ТекущиеДанные.ОтметкаСтроки Тогда
		Элементы.ВыделитьВсеСтрокиЗаказаЗакупки.Заголовок = "Отменить выделение всех";
		Элементы.ВыделитьВсеСтрокиЗаказаЗакупки.Картинка = БиблиотекаКартинок.СнятьФлажки; 
	КонецЕсли;
	Если ТекущиеДанные.Свойство("ОтметкаСтроки") И Не ТекущиеДанные.ОтметкаСтроки Тогда 
		Элементы.ВыделитьВсеСтрокиЗаказаЗакупки.Заголовок = "Выделить все";
		Элементы.ВыделитьВсеСтрокиЗаказаЗакупки.Картинка = БиблиотекаКартинок.УстановитьФлажки;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОтображениеКнопокДокументов(Элемент)
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Элементы.ПоказатьДокументЗаказПоставщику.Доступность = Ложь;
		Элементы.Заказать.Доступность = Ложь;
		Возврат;
	КонецЕсли;
	
	ДокументЕсть = Ложь;
	Если ТекущиеДанные.Свойство("ДокументЗаказПоставщика") Тогда
		ДокументЕсть = ЗначениеЗаполнено(ТекущиеДанные.ДокументЗаказПоставщика);
	КонецЕсли;
	
	ЭтоЗакупка = Ложь;
	Если ТекущиеДанные.Свойство("ВидОперации") Тогда
		ЭтоЗакупка = (ТекущиеДанные.ВидОперации = ПредопределенноеЗначение("Перечисление.Арм_ВидыОпераций.Закупка"));
	КонецЕсли;
	
	Элементы.ПоказатьДокументЗаказПоставщику.Доступность = ДокументЕсть;
	Элементы.Заказать.Доступность = Не ДокументЕсть И ЭтоЗакупка;
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	Попытка
		СброситьФлагиНаСервере();
	Исключение
	КонецПопытки;
КонецПроцедуры

&НаКлиенте
Процедура УдалитьТекущуюСтроку(Команда)
	УдалитьТекущуюСтрокуНаСервере();
	ОбновитьСписокПоТаймеру();
КонецПроцедуры
 
&НаСервереБезКонтекста
Процедура УдалитьТекущуюСтрокуНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи КАК ИдентификаторЗаписи,
		|	Арм_ДанныеЗакупокИПродаж.Номенклатура КАК Номенклатура,
		|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя КАК ДокументЗаказПокупателя
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|ГДЕ
		|	Арм_ДанныеЗакупокИПродаж.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Арм_ДанныеЗакупокИПродаж.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.ВРаботе)
		|	И Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
		|	И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &АвторСтроки
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи,
		|	Арм_ДанныеЗакупокИПродаж.Номенклатура,
		|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|ГДЕ
		|	Арм_ДанныеЗакупокИПродаж.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Закупка)
		|	И Арм_ДанныеЗакупокИПродаж.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.ВРаботе)
		|	И Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
		|	И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &АвторСтроки
		|	И Арм_ДанныеЗакупокИПродаж.РучнаяПравка
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи,
		|	Арм_ДанныеЗакупокИПродаж.Номенклатура,
		|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|ГДЕ
		|	Арм_ДанныеЗакупокИПродаж.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Продажа)
		|	И Арм_ДанныеЗакупокИПродаж.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.Черновик)
		|	И Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
		|	И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &АвторСтроки
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи,
		|	Арм_ДанныеЗакупокИПродаж.Номенклатура,
		|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|ГДЕ
		|	Арм_ДанныеЗакупокИПродаж.ВидОперации = ЗНАЧЕНИЕ(Перечисление.Арм_ВидыОпераций.Закупка)
		|	И Арм_ДанныеЗакупокИПродаж.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.Черновик)
		|	И Арм_ДанныеЗакупокИПродаж.ОтметкаСтроки
		|	И Арм_ДанныеЗакупокИПродаж.АвторСтроки = &АвторСтроки
		|	И Арм_ДанныеЗакупокИПродаж.РучнаяПравка";
	
	Запрос.УстановитьПараметр("АвторСтроки", ПараметрыСеанса.ТекущийПользователь);
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	
	ЗапросПроверки = Новый Запрос;
	ЗапросПроверки.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 2
		|	Арм_ДанныеЗакупокИПродаж.ИдентификаторЗаписи КАК ИдентификаторЗаписи
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
		|ГДЕ
		|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя = &ДокументЗаказПокупателя";
	
	Пока Выборка.Следующий() Цикл
		
		ЗапросПроверки.УстановитьПараметр("ДокументЗаказПокупателя", Выборка.ДокументЗаказПокупателя);
		Если ЗапросПроверки.Выполнить().Выгрузить().Количество() <= 1 Тогда
			Продолжить;
		КонецЕсли;
	
		Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
		Набор.Отбор.ИдентификаторЗаписи.Установить(Выборка.ИдентификаторЗаписи);
		Набор.Прочитать();
		Набор.Очистить();
		Попытка
			Набор.Записать();
		Исключение
			ЗаписьЖурналаРегистрации(
				"АРМ.УдалениеСтроки", 
				УровеньЖурналаРегистрации.Ошибка, 
				, 
				, 
				ОписаниеОшибки()
			);
		КонецПопытки;
		
		Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(Выборка.Номенклатура, Выборка.ДокументЗаказПокупателя);
	
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ТолькоСЗакупкамиПриИзменении(Элемент)
	
	УстановитьОтборПоЗакупкам();
	
КонецПроцедуры

&НаСервере
Процедура УстановитьОтборПоЗакупкам()
	
	ОтборСписка = ДанныеАРМОтображение.Отбор; 
	
	ГруппаОтбора = Неопределено;
	Для Каждого ЭлементОтбора Из ОтборСписка.Элементы Цикл
		Если ТипЗнч(ЭлементОтбора) = Тип("ГруппаЭлементовОтбораКомпоновкиДанных") 
			И ЭлементОтбора.Представление = "ГруппаТолькоЗакупки" Тогда
			ГруппаОтбора = ЭлементОтбора;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если ГруппаОтбора = Неопределено Тогда
		ГруппаОтбора = ОтборСписка.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		ГруппаОтбора.Представление = "ГруппаТолькоЗакупки";
	КонецЕсли;
	
	ГруппаОтбора.Использование = ТолькоСЗакупками;
	
	Если ГруппаОтбора.Элементы.Количество() = 0 Тогда
		
		Элемент = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		Элемент.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ЕстьДефицит");
		Элемент.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		Элемент.ПравоеЗначение = Истина;
		
		Элемент = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		Элемент.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Партия");
		Элемент.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;
		
	КонецЕсли;
	
	ХранилищеОбщихНастроек.Сохранить("АС_АРМ_Снабжение", "ТолькоСЗакупками", ТолькоСЗакупками);
	
КонецПроцедуры

&НаКлиенте
Процедура УдалитьЗаказПокупателя(Команда)
	Оповещение = Новый ОписаниеОповещения("ПослеВопросаОбУдаленииЗаказа", ЭтотОбъект);
	
	ПоказатьВопрос(
		Оповещение, 
		"Вы уверены, что хотите полностью удалить выбранные заказы покупателей?", 
		РежимДиалогаВопрос.ДаНет, 
		, 
		КодВозвратаДиалога.Нет,
		"Опасная операция: Удаление заказов"
	);
КонецПроцедуры

&НаКлиенте
Процедура ПослеВопросаОбУдаленииЗаказа(РезультатВопроса, ДополнительныеПараметры) Экспорт
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		УдалитьВыбранныеЗаказыНаСервере();
		ОповеститьОбИзменении(Тип("РегистрСведенийКлючЗаписи.Арм_ДанныеЗакупокИПродаж"));
	КонецЕсли;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура УдалитьВыбранныеЗаказыНаСервере()
	
	ЗапросСписокЗаказов = Новый Запрос;
	ЗапросСписокЗаказов.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Т.ДокументЗаказПокупателя КАК Заказ
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.ОтметкаСтроки = ИСТИНА
		|	И Т.АвторСтроки = &Автор
		|	И Т.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.Черновик)
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ РАЗЛИЧНЫЕ
		|	Т.ДокументЗаказПокупателя
		|ИЗ
		|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Т
		|ГДЕ
		|	Т.ОтметкаСтроки = ИСТИНА
		|	И Т.АвторСтроки = &Автор
		|	И Т.СтатусСтроки = ЗНАЧЕНИЕ(Перечисление.Арм_СтатусыАРМ.ВРаботе)";
	
	ЗапросСписокЗаказов.УстановитьПараметр("Автор", ПараметрыСеанса.ТекущийПользователь);
	ВыборкаЗаказов = ЗапросСписокЗаказов.Выполнить().Выбрать();
	
	Пока ВыборкаЗаказов.Следующий() Цикл
		
		ТекущийЗаказ = ВыборкаЗаказов.Заказ;
		
		ЗапросСвязи = Новый Запрос;
		ЗапросСвязи.Текст = 
			"ВЫБРАТЬ ПЕРВЫЕ 1
			|	ЗаказПоставщику.Ссылка КАК Ссылка
			|ИЗ
			|	Документ.ЗаказПоставщику КАК ЗаказПоставщику
			|ГДЕ
			|	ЗаказПоставщику.ЗаказПокупателя = &Заказ
			|	И ЗаказПоставщику.ПометкаУдаления = ЛОЖЬ";
		ЗапросСвязи.УстановитьПараметр("Заказ", ТекущийЗаказ);
		
		Если Не ЗапросСвязи.Выполнить().Пустой() Тогда
			ОбщегоНазначения.СообщитьПользователю(СтрШаблон("Заказ %1 пропущен: по нему есть активные закупки!", ТекущийЗаказ));
			Продолжить;
		КонецЕсли;

		СписокНоменклатуры = Новый Соответствие;
		ЗапросНоменклатура = Новый Запрос;
		ЗапросНоменклатура.Текст =
			"ВЫБРАТЬ РАЗЛИЧНЫЕ
			|	Арм_ДанныеЗакупокИПродаж.Номенклатура КАК Номенклатура
			|ИЗ
			|	РегистрСведений.Арм_ДанныеЗакупокИПродаж КАК Арм_ДанныеЗакупокИПродаж
			|ГДЕ
			|	Арм_ДанныеЗакупокИПродаж.ДокументЗаказПокупателя = &Заказ";
		ЗапросНоменклатура.УстановитьПараметр("Заказ", ТекущийЗаказ);
		ВыборкаНоменклатура = ЗапросНоменклатура.Выполнить().Выбрать();
		Пока ВыборкаНоменклатура.Следующий() Цикл
			Если ЗначениеЗаполнено(ВыборкаНоменклатура.Номенклатура) Тогда 
				СписокНоменклатуры.Вставить(ВыборкаНоменклатура.Номенклатура, Истина); 
			КонецЕсли;
		КонецЦикла;

		ОбъектЗаказа = ТекущийЗаказ.ПолучитьОбъект();
		Если ОбъектЗаказа <> Неопределено И Не ОбъектЗаказа.ПометкаУдаления Тогда
			Попытка
				ОбъектЗаказа.УстановитьПометкуУдаления(Истина);
			Исключение
				ТекстСообщения = СтрШаблон(
					"Не удалось удалить заказ %1 по причине: %2",
					Строка(ТекущийЗаказ),
					ОписаниеОшибки()
				);
				ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
				Возврат;
			КонецПопытки;
		КонецЕсли;

		Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
		Набор.Отбор.ДокументЗаказПокупателя.Установить(ТекущийЗаказ);
		Попытка
			Набор.Записать();
		Исключение
			ЗаписьЖурналаРегистрации("АРМ.УдалениеЗаказа", УровеньЖурналаРегистрации.Ошибка,,, ОписаниеОшибки());
		КонецПопытки;

		Для Каждого КлючИЗначение Из СписокНоменклатуры Цикл
			Арм_ОбщегоНазначенияАРМ.ПолучитьОстаткиПоСтрокам(КлючИЗначение.Ключ, Документы.ЗаказПокупателя.ПустаяСсылка());
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура СкрытьОтгруженныеПриИзменении(Элемент)
	СкрытьОтгруженныеНаСервере()
КонецПроцедуры

&НаСервере
Процедура СкрытьОтгруженныеНаСервере()
	
	Отбор = ДанныеАРМОтображение.Отбор.Элементы;
	
	ПолеОтбора = Новый ПолеКомпоновкиДанных("СтатусСтроки");
	НужныйЭлемент = Неопределено;
	
	Для Каждого ЭлементОтбора Из Отбор Цикл
		Если ТипЗнч(ЭлементОтбора) = Тип("ЭлементОтбораКомпоновкиДанных") 
			И ЭлементОтбора.ЛевоеЗначение = ПолеОтбора Тогда
			НужныйЭлемент = ЭлементОтбора;
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Если НужныйЭлемент = Неопределено Тогда
		НужныйЭлемент = Отбор.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		НужныйЭлемент.ЛевоеЗначение = ПолеОтбора;
	КонецЕсли;
	
	НужныйЭлемент.Использование  = СкрытьОтгруженные;
	НужныйЭлемент.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	НужныйЭлемент.ПравоеЗначение = Перечисления.Арм_СтатусыАРМ.Отгружено;
	
	ХранилищеОбщихНастроек.Сохранить("АРМ_Автосервис_Настройки", "СкрытьОтгруженные", СкрытьОтгруженные);

КонецПроцедуры

&НаКлиенте
Процедура ВыделитьВсеСтрокиЗаказа(Команда)
	ДанныеСтроки = Элементы.ДанныеАРМОтображение.ТекущиеДанные;
	Если ДанныеСтроки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	ОбработатьНажатиеФлагиВыделения(ДанныеСтроки);
	Если ДанныеСтроки.Свойство("ОтметкаСтроки") И ДанныеСтроки.ОтметкаСтроки Тогда
		Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Отменить выделение всех";
		Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.СнятьФлажки; 
	КонецЕсли;
	Если ДанныеСтроки.Свойство("ОтметкаСтроки") И Не ДанныеСтроки.ОтметкаСтроки Тогда 
		Элементы.ВыделитьВсеСтрокиЗаказа.Заголовок = "Выделить все";
		Элементы.ВыделитьВсеСтрокиЗаказа.Картинка = БиблиотекаКартинок.УстановитьФлажки;
	КонецЕсли;
	ОбновитьСписокПоТаймеру();
КонецПроцедуры

&НаКлиенте
Процедура ВыделитьВсеСтрокиЗаказаЗакупки(Команда)
	ДанныеСтроки = Элементы.ДанныеАРМОтображениеЗакупки.ТекущиеДанные;
	Если ДанныеСтроки = Неопределено Тогда
		Возврат;
	КонецЕсли;
	ОбработатьНажатиеФлагиВыделения(ДанныеСтроки);
КонецПроцедуры

&НаКлиенте
Процедура ОбработатьНажатиеФлагиВыделения(ДанныеСтроки)

	Если Не ДанныеСтроки.Свойство("ДокументЗаказПокупателя") И Не ЗначениеЗаполнено(ДанныеСтроки.ДокументЗаказПокупателя)
		Или ДанныеСтроки.Свойство("ГруппировкаСтроки") Тогда
		ТекстСообщения = "Для того, чтобы отметить все галки встаньте на строку, а не на группировку";
		ПоказатьОповещениеПользователя("Внимание!", , ТекстСообщения);
		Возврат;
	КонецЕсли;
	НовоеЗначениеФлага = Не ДанныеСтроки.ОтметкаСтроки;
	ВыделитьВсеСтрокиЗаказаНаСервере(ДанныеСтроки.ДокументЗаказПокупателя, ДанныеСтроки.ВидОперации, НовоеЗначениеФлага);
	//ОбновитьСписокПоТаймеру();
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ВыделитьВсеСтрокиЗаказаНаСервере(ДокЗаказПокупателя, ВидОперации, НовоеЗначениеФлага)
	
	АвторДляЗаписи = ?(НовоеЗначениеФлага, ПараметрыСеанса.ТекущийПользователь, Справочники.Пользователи.ПустаяСсылка());
	
	Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
	Набор.Отбор.ДокументЗаказПокупателя.Установить(ДокЗаказПокупателя);
	Набор.Прочитать();
	
	Изменено = Ложь;
	Автор = ПараметрыСеанса.ТекущийПользователь;
	
	Для Каждого Запись Из Набор Цикл
		
		СтрокаСвободна = Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка() Или Запись.АвторСтроки = Автор; 
		Если (Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.ВРаботе
			Или Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Черновик)
			И Не Запись.Удаление И СтрокаСвободна Тогда
			
			Если Запись.ВидОперации = ВидОперации Тогда
				Запись.ОтметкаСтроки = НовоеЗначениеФлага;
				Запись.АвторСтроки   = АвторДляЗаписи;
				Изменено = Истина;
			КонецЕсли;
			
		КонецЕсли;
	КонецЦикла;
	
	Если Не Изменено Тогда Возврат КонецЕсли;
	
	Попытка
		Набор.Записать();
	Исключение
		ТекстОшибки = СтрШаблон(
			"Ошибка записи в АРМ-регистр. Выделение всех строк пользователем %1, подробно: %2", 
			ПараметрыСеанса.ТекущийПользователь, 
			КраткоеПредставлениеОшибки(ИнформацияОбОшибке())
		);
		ЗаписьЖурналаРегистрации(
			"АРМ.ВыделениеВсехСтрок", 
			УровеньЖурналаРегистрации.Ошибка, 
			, 
			, 
			ТекстОшибки
		);
	КонецПопытки;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаполнитьДатуИНомерОтгрузкиДляЗаказа(Команда)
	
	ДанныеСтроки = Элементы.ДанныеАРМОтображение.ТекущиеДанные;
    
    Если ДанныеСтроки = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    ОписаниеОповещения = Новый ОписаниеОповещения("ПослеВводаРеквизитовОтгрузки", ЭтотОбъект, ДанныеСтроки.ДокументЗаказПокупателя);
    ОткрытьФорму(
		"Обработка.АС_АРМ2.Форма.ФормаВводаРеквизитовОтгрузки", 
		, ЭтотОбъект, 
		, 
		, 
		, 
		ОписаниеОповещения, 
		РежимОткрытияОкнаФормы.БлокироватьОкноВладельца
	);
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаРеквизитовОтгрузки(РезультатВвода, ДополнительныеПараметры) Экспорт
    
    Если РезультатВвода = Неопределено Тогда
        Возврат;
    КонецЕсли;
    
    ДокументЗаказ = ДополнительныеПараметры;
    
    ОбновитьДанныеОтгрузкиВРегистреНаСервере(ДокументЗаказ, РезультатВвода.ДатаОтгрузки, РезультатВвода.НомерОтгрузки);
    ОбновитьСписокПоТаймеру();
    
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбновитьДанныеОтгрузкиВРегистреНаСервере(Знач ДокументЗаказ, Знач НоваяДата, Знач НовыйНомер)
    
    Если Не ЗначениеЗаполнено(ДокументЗаказ) Тогда
        Возврат;
    КонецЕсли;
    
    Набор = РегистрыСведений.Арм_ДанныеЗакупокИПродаж.СоздатьНаборЗаписей();
    Набор.Отбор.ДокументЗаказПокупателя.Установить(ДокументЗаказ);
    Набор.Прочитать();
    
    Если Набор.Количество() > 0 Тогда
		
		Автор = ПараметрыСеанса.ТекущийПользователь;
		Изменено = Ложь;
        Для Каждого Запись Из Набор Цикл
			
			Если ЗначениеЗаполнено(Запись.Партия) Тогда
				СтрокаСвободна = Запись.АвторСтроки = Справочники.Пользователи.ПустаяСсылка() Или Запись.АвторСтроки = Автор; 
				Если (Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.ВРаботе
					Или Запись.СтатусСтроки = Перечисления.Арм_СтатусыАРМ.Черновик)
					И Не Запись.Удаление И СтрокаСвободна Тогда
					
		            Запись.ДатаОтгрузки = НоваяДата;
		            Запись.НомерОтгрузки = НовыйНомер;
					Изменено = Истина;
					
				КонецЕсли;
			КонецЕсли;
            
        КонецЦикла;
		
		Если Не Изменено Тогда Возврат КонецЕсли;
		
		Попытка
            Набор.Записать(); 
        Исключение
            ЗаписьЖурналаРегистрации(
				"АРМ.ОшибкаМассовойЗаписиНабораОтгрузки", 
                УровеньЖурналаРегистрации.Ошибка, 
				, 
				, 
				ОписаниеОшибки()
			);
        КонецПопытки;
        
    КонецЕсли;
    
КонецПроцедуры

&НаКлиенте
Процедура ДанныеАРМОтображениеОтметкаСтрокиПриИзменении(Элемент)
	//ТекущиеДанные = Элементы.ДанныеАРМОтображение.ТекущиеДанные;
	//Если ТекущиеДанные = Неопределено Тогда 
	//	Возврат; 
	//КонецЕсли;
	//Если Не ТекущиеДанные.Свойство("ИдентификаторЗаписи") Или Не ТекущиеДанные.Свойство("ОтметкаСтроки") Тогда
	//	Возврат;
	//КонецЕсли;
	//ИнвертироватьФлагНаСервере(ТекущиеДанные.ИдентификаторЗаписи, ТекущиеДанные.ОтметкаСтроки);
КонецПроцедуры

&НаКлиенте
Процедура ПоказатьДокументРасходнаяНакладная(Команда)
	СписокДокументов = ПолучитьСписокНужныхДокументовДляОткрытия("РасходнаяНакладная");
    Для Каждого ДокументСсылка Из СписокДокументов Цикл
        ПоказатьЗначение(, ДокументСсылка);
	КонецЦикла;
	Если СписокДокументов.Количество() = 0 Тогда
		ТекстСообщения = "Для выделенных строк отсутствует документ расходная накладная";
		ПоказатьОповещениеПользователя("Не данных для отображения", , ТекстСообщения);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ГруппироватьПоНомеруЗаказаКлиентаПриИзменении(Элемент)
	
	Если ГруппироватьПоНомеруЗаказаКлиента Тогда
		СгруппироватьПоЗаказуПокупателя = Ложь;
	КонецЕсли;
	
	УстановитьГруппировкуАРМНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура УстановитьГруппировкуАРМНаСервере()
    
    Настройки = ДанныеАРМОтображение.КомпоновщикНастроек.Настройки;
    Настройки.Структура.Очистить();
    
    Если СгруппироватьПоЗаказуПокупателя Тогда
		
        ГруппировкаПоЗаказу = Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
        ПолеГрупп = ГруппировкаПоЗаказу.ПоляГруппировки.Элементы.Добавить(Тип("ПолеГруппировкиКомпоновкиДанных"));
        ПолеГрупп.Поле = Новый ПолеКомпоновкиДанных("ДокументЗаказПокупателя");
        ПолеГрупп.Использование = Истина;
        
        Детальные = ГруппировкаПоЗаказу.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
		
    ИначеЕсли ГруппироватьПоНомеруЗаказаКлиента Тогда
		
		ГруппировкаПоНомера = Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
        ПолеГрупп = ГруппировкаПоНомера.ПоляГруппировки.Элементы.Добавить(Тип("ПолеГруппировкиКомпоновкиДанных"));
        ПолеГрупп.Поле = Новый ПолеКомпоновкиДанных("НомерЗаказаКлиента");
        ПолеГрупп.Использование = Истина;
        
        Детальные = ГруппировкаПоНомера.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
		
    Иначе
		
        Детальные = Настройки.Структура.Добавить(Тип("ГруппировкаКомпоновкиДанных"));
		
    КонецЕсли;
    
    Детальные.Использование = Истина;
    Детальные.Выбор.Элементы.Добавить(Тип("АвтоВыбранноеПолеКомпоновкиДанных"));
    
    ДанныеАРМОтображение.КомпоновщикНастроек.ЗагрузитьНастройки(Настройки);
	
	ХранилищеОбщихНастроек.Сохранить("АРМ_НастройкаГруппировки", "ФлагГруппировкиЗаказ1С", СгруппироватьПоЗаказуПокупателя);
	ХранилищеОбщихНастроек.Сохранить("АРМ_НастройкаГруппировки", "ФлагГруппировкиЗаказКлиента", ГруппироватьПоНомеруЗаказаКлиента);

КонецПроцедуры

#КонецОбласти